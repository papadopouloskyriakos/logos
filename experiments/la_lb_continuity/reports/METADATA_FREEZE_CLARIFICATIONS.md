# METADATA_FREEZE_CLARIFICATIONS — §II (membership UNCHANGED)

_Generated by `src/continuity/partitions.py`. These are metadata-only re-expressions of the already
frozen packets — **no candidate/target membership changes**. Frozen input hashes verified at run
(`cfg.verify_inputs`); a drift would STOP the pass._

## 1. Mutually-exclusive base partitions (packet A)
```
PRIMARY_B                 = 11   (tier-B TOPONYM_LIKE)
SENSITIVITY_C_ADDITIONAL  = 33   (tier-C TOPONYM_LIKE — ADDITIONAL to PRIMARY_B, not overlapping)
NEGATIVE_CONTROL          = 21
QUARANTINED               =  1   (I-DA, contaminated control)
TOTAL                     = 66
```
Each unique form belongs to **exactly one** base partition (tested). Sensitivity analyses use
`PRIMARY_PLUS_SENSITIVITY = PRIMARY_B ∪ SENSITIVITY_C_ADDITIONAL = 44`, constructed programmatically
(deterministic, sorted by candidate_id) — never stored as an overlapping count.

## 2. ARKH ambiguity policy (frozen)
3 candidates sit on the `Arkhalkhori`(=Arkalochori) / `Arkhanes` cross-source disagreement. Rule:
- retained in corpus-wide analyses if all other eligibility passes;
- `site_assignment = AMBIGUOUS_ARKH` — **never** opportunistically assigned to either site;
- **excluded** from any leave-one-site-out step that requires an unambiguous site, and reported
  separately;
- assignment-specific sensitivity only if prespecified; the stronger-result assignment is never chosen.

## 3. Unit of analysis + duplicate dependence (frozen)
- **Primary statistical unit = unique word form** (one score per form). 66 unique forms; 254 raw
  document attestations; **effective independent units = 66** (tablets never multiply the count).
- 32 single-site repeated-form clusters (flagged; formulaic-repeat risk) · 32 cross-site forms
  (cross-site recurrence is a prespecified feature, not independent lexical discoveries).
- Significance is computed by unique form, with clustered/block resampling by form/site where relevant
  — never as though each tablet occurrence were independent.

## Interpretive frame (per the authorization)
The primary channel is **administrative**. The five known pairs are mostly ritual/localized/absent, so
their `0/5` overlap with PRIMARY/SENSITIVITY is a **domain mismatch**, not a positive-control failure.
The five are used only as `DEVELOPMENT_BENCHMARK` / `SOURCE_CRITICAL_DIAGNOSTIC` /
`OUT_OF_DOMAIN_CHECK`. Primary question: *do internally-selected administrative TOPONYM_LIKE Linear A
forms show orthographic/lexical continuity with independently-selected Linear B Cretan toponyms beyond
matched end-to-end nulls?*
