# FINAL — Di Mino `*301=/na/` exact mechanical verdict

**Branch:** `research/di-mino-301-exact-audit` · **Parent:** `main@6fd4f20` · **Constitution:** v2.2 ·
**Opened/Closed:** 2026-07-08. This is the exact mechanical adjudication the LOGOS paper identified as missing —
not a re-run of the qualitative audit.

> ## `DI_MINO_CORE_VERDICT = REJECT`
> The specific chain **`*301=/na/ → root N-W-Y → gloss "dwell" → Semitic`** fails the frozen, pre-registered,
> multiplicity-deflated, decontaminated, held-out gate on ≥3 load-bearing clauses, on a pipeline **proven
> calibrated** (positive controls PASS: it recovers real and planted truth and rejects a manufactured fake). The
> **end-to-end false-positive rate is 1.0** (search-adjusted p ≈ 1.0): the same pipeline manufactures an equally
> strong chain on essentially every null run, so the lexical/gloss legs are `NULL_PUBLISHED` — the claim *is* the
> search null. Because PC passed, this is a **genuine negative**, not a dead-detector artifact.
>
> **Scope (binding):** this rejects the *chain*. It does **not** touch the structural **i-*301 core** (the
> mainstream Davis/Thomas reading — never Di Mino's novelty) or the invocation-slot **function**. It does **not**
> claim any historical Semitic etymology is universally impossible — only that the advertised match is **not exact
> under the frozen correspondence rules**. Extended claims H7–H12 are `SOURCE_BLOCKED`, **not** refuted.

## 1–2. Commits & isolation
Parent `main@6fd4f20` (untouched); final commit = this campaign's HEAD on `research/di-mino-301-exact-audit`;
worktree `/…/logos-di-mino-301-audit`. Paper byte-frozen (arxiv sha256 `a2ab89fa…`); `paper/`, `runtime/`, CSA,
completed branches untouched; test baseline 89 passed; append-only ledger active; not merged to `main`.

## 3–5. Prior-test census & artifacts
The **exact core gate had never been run** (census reports/00 — the paper names the gap verbatim). Public
artifacts obtained + hashed: the AI Clambake article (CLB-01) and **Figure 1** (`minoan-fig1.png` sha256
`651e50d9…`). Missing (checked article/GitHub/minoanmystery.org): 40-sign table, 408-lexicon, "Ya Diktu"
manuscript, 5 LB corrections, logogram analysis, ~100k-simulation code → **all `SOURCE_BLOCKED`**.

## 6–7. Claim graph & multiplicity
Load-bearing circularity `e2→e3→e4`: `*301`'s value is chosen so `na-wa-ya` yields `n-w-y`, then `n-w-y` is
offered as evidence for the value. Figure 1 confirms **5/6 signs are conventional transferred LB values (score 0);
the entire novel content is the single unanchored parameter `*301=/na/`**. Lower-bound search receipt: ≥55 values
× ≥4 segmentations × ≥10³ roots/lang × ≥6 languages × ≥3 glosses; plus the author's **own stated ~10⁵ simulations**
as a published multiplicity the deflated bar must absorb. Logged `N_eff` (this audit) = 260 value×lexeme×seg cells.

## 8–9. Corpus & segmentation
36 `*301` attestations / 23 distinct forms / 31 libation carriers; partition 4 derivation / 32 held-out (26
leave-site-out-eligible). **Integrity flag:** the prereg-named held-out anchor IOZa6 sits on derivation site
Iouktas → leave-form-out only. **Segmentation is decisive against the claim:** no value-free family reproduces the
`*301-WA-JA` root boundary; the key cut b3 (before `*301`) has **forward branching-entropy 0.00** (A-TA-I is
*always* followed by `*301` — the most anti-boundary internal position); Viterbi/MDL keeps the word whole. The
root boundary is phonetically motivated (corroborating the circularity).

## 10–12. `*301` value audit (H1)
**`REJECT`.** Primary `S_morph` is **value-invariant** (identical 0.2738 under any relabeling) →
`NO_DISCRIMINATIVE_POWER_FOR_VALUE` (not itself a rejection). On the independent lexical channel, `/na/` ranks
**21/65** (20 strictly better, 30 tied; **49th percentile** of the Packard permutation null; deflated 0.0).
Leave-target-out: rank 21 (unchanged). Leave-site/form-out: never rank 1 (range 6–26). `/na/` is #1 only on the
weakest `S_phono` by a negligible 0.095-nat margin (clustered with ya/ka/ra/pa). Fails `E[max_Neff]` over the
logged 260 cells **and** the author's 10⁵ (0.667 < 0.833). Power ADEQUATE (32 held-out forms). Independent
structural red flag (E): `/na/` is already written **158×** by the standard NA sign (AB06), 3× in the same IOZa2
line — a second `/na/` sign is typologically implausible.

## 13. Root & gloss (H2/H3)
**`REJECT`.** Root existence is a **generic search outcome** — 31% (strict n-w-y) to 77% (weak+hollow) of
admissible `*301` C₁ onsets hit a real Semitic root. The gloss "dwell" is **inexact under the frozen rules**:
`na-wa-ya` gives final **yod** (n-w-y), but Hebrew "dwell" is n-w-**h** (final he; *navah/naveh* "abode/pasture",
mostly nominal) — reaching "dwell" requires an **unregistered III-y → III-h correspondence transformation**
(charged to the receipt; not in the frozen rules). Language-cherry-picked: the yod-final attested roots are
Arabic *nawā* "intend/journey" and Ugaritic "drive away", **not** "dwell". (This does not claim the etymology is
universally impossible — only non-exact under the frozen rules.)

## 14. Morphology & paradigm (H5)
**`REJECT`.** `MORPH_ROOT` refuted (the post-`*301` sign varies WA/U/TI/DE/NA/SI → `*301` heads no stable root;
zero held-out recurrence beyond target copies); `PARADIGM` reject (the target-derived template predicts its own
"invariant root" in only 1/6 held-out forms); `SEMITIC_MORPHOLOGY` not supported. Head-to-head: **Davis I-*301
stem 0.833 (best) beats Di Mino's root 0.167 (worst)**; Thomas -JA/-E ending corroborated (0.50). Report separate:
MORPH_A / MORPH_TA survive only at L3 as generic onset/affix presence and favour the Davis reading.

## 15. Full formula (E)
34/35 publicly-adjudicable signs are conventional LB transliteration (`literature_match`, score 0); exactly one
novel parameter (`*301=/na/`). Only word 1 has a published morpheme table; words 2–8 `SOURCE_BLOCKED`.

## 16–18. Extended artifacts (H7/H8/H9/H10/H11)
All `SOURCE_BLOCKED` (reports F/G/H) — tables/lexicon/corrections not public. **Not refutations; not collapsed
into the core verdict.**

## 19. Positive controls
**PASS — pipeline CALIBRATED.** PC1 Ugaritic→Hebrew recovered (0.780 vs L_fake floor 0.357); PC2 opaque LB→Greek
recovered (0.923 vs permuted p95 0.385, 2.7×); **PC3 planted `*301`-style value recovered rank 1/113 (margin
0.963)**; PC4 manufactured-fake Semitic REJECTED; PC5/PC6 distinguish agglutinative (z 28.8) vs Semitic morphology.
The L_fake floor is >0 and < the positive benchmark in every recover control.

## 20–21. End-to-end false-positive rate & search-adjusted significance
**End-to-end FP rate = 1.0.** The full adaptive pipeline (best-of-{value, segmentation, weak-collapse, language,
gloss}) manufactures a match at least as strong as the observed `/na/→N-W-Y→"dwell"` chain on **essentially every**
null run: 0.769 per single random value before any language search; best-of-search 1.0; the "dwell"-tier hit is
**certain over the 7,800 logged root/gloss cells**. **Search-adjusted p ≈ 1.0** — the whole-chain significance is
nil. 10/13 admissible `*301` onsets already yield a real Semitic weak/hollow root with a citable gloss. The
observed chain is therefore **indistinguishable from the fake/root-search null on the lexical/gloss legs** →
those legs are `NULL_PUBLISHED`. The graduation gate's own false-graduation rate is 0.006 (0.6%, calibrated) and
the claim does not approach it. Because the positive controls PASSED (PC3 recovers a planted value at rank 1/113),
this null-indistinguishability is a **genuine negative**, not a dead detector. The edit-distance channel is itself
`NO_POWER` (L_fake floor 1.0 frozen / 0.99 reconstructed) — root existence rests on the BDB-cited lexical table,
not on a "match-exists" statistic.

**Composite:** value `S_morph` = `NO_DISCRIMINATIVE_POWER`; value `S_lex` = `REJECT` (rank 21/65, fails rank-1 with
power); lexical/gloss legs (H2/H3) = `NULL_PUBLISHED`; **core chain = `REJECT`** (fails ≥3 load-bearing clauses:
value rank-1, H2 held-out recurrence 0/3, H3 beats-features 0/3).

## 22–23. Surviving vs failed subclaims (frozen matrix)
| H | subclaim | verdict |
|---|---|---|
| H1 | `*301`=/na/ | **REJECT** (unconditional — PC passed) |
| H2 | root N-W-Y | **REJECT** (generic search outcome; no held-out recurrence) |
| H3 | gloss "dwell" | **REJECT** (inexact under frozen rules; III-y→III-h unregistered; cherry-picked) |
| H4 | invocation-verb / **function** | **PARTIAL_SUPPORT (L3 functional only)** — formula-initial inflected word with separable onset/ending; = the Davis/Thomas structural core, NOT Di Mino's novelty; no phonetic licence |
| H5 | exact Semitic morphological parse | **REJECT** |
| H6 | regional paradigm | **NO_POWER** (no regional generator; ties baselines) |
| H7–H12 | 40 signs / 13 LA-only / 408 lexicon / 5 LB / logograms / reads-outside | **SOURCE_BLOCKED** |

**Surviving:** only the value-free L3 structural residue (i-*301 core + invocation-slot function), which is the
mainstream reading, not the disputed claim. **Failed:** the entire `/na/ → N-W-Y → "dwell" → Semitic` chain.

## 24–25. Verdicts
`DI_MINO_CORE_VERDICT = REJECT`. Extended: `FULL_SIGN_MAP_VERDICT = SOURCE_BLOCKED`;
`FULL_LEXICON_VERDICT = SOURCE_BLOCKED`; `LINEAR_B_CORRECTIONS_VERDICT = SOURCE_BLOCKED`;
`LOGOGRAM_CORRECTIONS_VERDICT = SOURCE_BLOCKED`.

## 26. Highest earned LOGOS claim layer
For the Di Mino claim: **L3 (functional/structural) only**, and only for the value-free residue that coincides
with the mainstream Davis/Thomas reading. **No phonetic (L6), lexical (L5), semantic (L4), or language-ID (L8)
layer is earned.** No transfer licence.

## 27. Integrity confirmation
The submitted paper and all prior campaign history remained untouched; every finding is append-only; the
qualitative audit was reproduced/cited, not altered; the two frozen preregistered one-shots were not re-run; no
sign reading was advanced in LOGOS's own voice; no LLM graded the claim.
