# Checkpoint 02 — wave 2 (substitution audit + F + G); the central result

**Commit:** f99eca6 · **as_of:** 2026-07-07 · licences: all NOT_EARNED.

## Central finding
- The substitution consonant-held axis is a GENUINE, twice-audited relative-structure signal on opaque LB
  (`C_AUDIT` VALIDATED; survives the exact battery that refuted position — 4/4 implementations, freq-band-disjoint,
  freq-matched null p=0.0033). => internal methods are NOT universally value-blind, via SUBSTITUTION not position.
- BUT Linear A can't use it: `C4/C5` NOT_RECOVERED (underpowered support-3-vs-98; word-INITIAL wrong-signature;
  FRAGILE; deflated info-gain 4 bits). Cross-script `F` NULL (decomposed: only admin_function non-circular, =NULL).
  Anchors `G` SEED_A=0.

## Complete this checkpoint
- C4, C5, C_AUDIT, F1, F2, F4, G1, G2, G3 (+ WP-A's 6 in checkpoint prior).

## Next (wave 3, unblocked)
- B5→B6 (LA seg ensemble + downstream causal), D1/D2/D3/D4/D5 (D3 = audit the Foundry '3-4 seeds->0.87'),
  E1-E5 (morphology + formula grammar). Then wave 4 H/I/J2, wave 5 K/M.

## Trajectory
NEW_CONSTRAINTS_BUT_NO_DECIPHERMENT with a sharper, twice-audited characterization: value-blindness is FALSE in
general (substitution) but LA's value layer is closed by power + word-initial typology + empty non-circular anchors.
