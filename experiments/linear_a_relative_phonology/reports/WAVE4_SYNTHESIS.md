# Wave 4 synthesis — offensive closed (all null); L2/L3 positive validated on held-out data

## Candidate rounds + agnostic search: comprehensively null, with the identifiability ceiling shown directly
- **H1 (relative-class-constrained) → `AT_END_TO_END_NULL`.** 0/3 serious families (West Semitic, Anatolian/
  Luwic, Tyrsenian) beat the multi-family end-to-end null on 1,074 held-out multi-sign LA words. SEM raw-clears
  (FWER 0.0149) but only on the single lexeme KU-RO='total' (fails Holm 0.0745; leave-one-lexeme-out → rate 0.0).
  TYR highest raw (0.0726) but inside the random-lexicon band (shape typicality). The negative-control real
  language (Finnish, impossible for MBA Crete, decisive 0.915) and the agnostic control (0.527) both sit at the
  null → the bar is specific, not leaky.
- **H2 (anchor-constrained) → `AT_ANCHOR_CONSTRAINED_NULL`.** Supplying the WP-G audited anchors adds 0 held-out
  matches for every family; pinning 17 anchor sign-values covers 135/1067 words but yields 0 lexical readings
  (SEED_A=0 demonstrated directly, one-toponym-deep). All anchors circular per Art. XII.
- **H3 (morphology-first joint) → `AT_JOINT_INFERENCE_NULL` — the decisive round.** It does not bet on a language;
  it measures identifiability directly. **The morphology-first backbone is relabeling-invariant: 200 consistent
  value-map relabelings leave the held-out read-count EXACTLY invariant (std 0) → 0 bits of value information.**
  The Bayesian joint posterior is flat (3.585 bits = 12 equiprobable assignments), the CSP feasible set pins only
  {A, KU} by external-lexeme choice (0 held-out coverage), MDL commits only KU-RO. This proves R1/R2 were not
  contingent bad luck — the value layer is genuinely underdetermined given the available evidence.
- **I1 (agnostic constrained search) → `UNDERDETERMINED_NO_RECOVERY`.** The detector is LIVE (LB control recovers
  its consonant grid, AMI 0.504, perm-p 0.024 — matching wave-2), so the LA null is informative. LA's one nominal
  signal (C15 consonant grade p=0.046) is refuted three ways (permuted-corpus winner matches it, grid-unstable,
  dies under Holm). **Equivalence-class accounting: any LA value system has ≥10^27 relabeling-equivalent twins,
  breakable only by an external anchor/bilingual — and SEED_A=0.** The information-theoretic statement of the
  obstacle.

## Sealed predictions: the L2/L3 positive PASSES held-out structural tests
`J2` built 5 hashed seals. Since no phonetic value candidate exists, phonetic scoring is `NO_CANDIDATE_TO_SEAL`,
but the STRUCTURAL predictions from the validated morphology/formula grammars are scoreable — and 3/4 non-
prospective seals are SUPPORTED:
- **SEAL_2 (unseen 15% inscriptions): SUPPORTED** — held-out A-prefix rate 0.0525 inside the train band; ledger
  well-formedness 0.857.
- **SEAL_3 (unseen site Khania): SUPPORTED (the substantive result)** — the abstract carrier→value ledger grammar
  transfers cleanly (well-formed 0.8525) while the KU-RO='total' lexeme is correctly predicted ABSENT (0 carriers
  at Khania — it is HT-specific). This cleanly separates transferable grammar from a site-specific lexeme.
- **SEAL_4 (unseen libation family, LOO): SUPPORTED** — the rigid anchor order OP<SSR<UNK<IPN<SIR survives
  leave-one-carrier-out with 0/45 inversions.
- **SEAL_5 (masked notation): honest committed NEGATIVE** — ledger arithmetic recovery of masked numerals does not
  beat a mode baseline (n=6, silver drops fraction glyphs; consistent with E2's 7/31 exact ceiling).
- **ANETAKI_FINAL_EDITION_DELTA_SEAL:** prospective, structural-only, excludes every J1-flagged public item —
  predicts A-prefixation rate + carrier-value/libation structure in the unpublished faces, scoreable when the
  editio princeps lands.

## Net
The offensive value-recovery attempt is comprehensively and honestly closed (`AT_END_TO_END_NULL` /
`AT_JOINT_INFERENCE_NULL` / `UNDERDETERMINED_NO_RECOVERY`), with the identifiability ceiling demonstrated directly
(relabeling-invariance → 0 bits; ≥10^27 twins). The durable positive — anonymous L2/L3 administrative structure —
is validated on held-out inscriptions, an unseen site, and an unseen formula family. Highest claim layer L2/L3;
all transfer licences NOT_EARNED; no phonetic value assigned anywhere.
