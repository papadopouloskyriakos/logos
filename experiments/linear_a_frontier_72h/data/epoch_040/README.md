# EPOCH-040 data artifacts (data/epoch_040/)

- `per_site.csv` — per-site (>=50 words) rep_rate, length-matched i.i.d.-unigram null mean,
  two-sided p, direction, significance flag.
- `summary.json` — global, positive-control, cross-site, LOO, analytic cross-check, verdict.

All numbers generated by `experiments/linear_a_frontier_72h/epochs/EPOCH-040/machinery.py`
(seeds fixed; 2000 draws for global/per-site/LOO; 2000 detect / 1000 FP-per-set / 20 FP sets
for the positive control). Signs are ANONYMOUS; pure L2/L3 distributional statistics.
