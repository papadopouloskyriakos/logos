# PREREGISTRATION — Di Mino `*301=/na/` exact claim · V1

**Frozen 2026-07-08 BEFORE any outcome is observed.** Content-hashed in `manifests/DI_MINO_EXACT_CLAIM_V1.sha256`.
Any revision → V2 with a new hash + a new held-out target. Constitution v2.2. Non-circular: known values grade
benchmarks only. Primary held-out statistic throughout: `S_morph` (recurring-morphology); `S_lex`/`S_phono`
reported but never sufficient alone. Deflation: `E[max_Neff]` over the logged `N_eff` (comparison-layer §B).

## Global partitions
- **Derivation set (frozen religious language, used by the claim):** the Libation-Formula attestations Di Mino
  derives on, incl. IOZa2 (Iouktas), TL Za 1, and the `i-*301` core forms.
- **Held-out set:** Libation-Formula carriers at sites NOT used to fix the value/morphology + all admin tablets
  (HT series etc.), + multi-attestation forms `TA-NA-I-*301-U-TI-NU` (IO Za 6), `A-TA-I-*301-DE-KA` (ZA Zb 3)
  under leave-form-out. No site/form/edition may appear in both partitions via aliases or joins.
- **Grouping unit:** inscription; cross-validation unit = peak-sanctuary site (the natural 5-fold formula CV).

## Core hypotheses

**H1 — `*301`=/na/ (or a /na/-initial value).**
- success: `/na/` ranks above the preregistered admissible value space on held-out `S_morph`+`S_lex`, AND survives
  leave-target-out (remove A-TA-I-*301-WA-JA) AND eligible leave-site-out, AND clears `E[max_Neff]`.
- null: frequency-banded sign-value permutation (Packard) + random-lexeme draw + L_fake canary.
- multiplicity: deflate for `N_eff` = logged (value × lexeme × segmentation) trials + report vs author's ~10⁵.
- min effect: DSR ≥ 0.95 AND rank(/na/)=1 with margin > next-best.
- failure: /na/ not top-ranked, or fails leave-target-out, or inside the null band.
- NO_POWER: < ~3 independent held-out `*301` forms after partition (can't discriminate values).
- authorized: "/na/ {is|is not} specifically supported over alternatives"; forbidden: any claim about the language.

**H2 — A-TA-I-*301-WA-JA contains root N-W-Y.** success: the committed map applied blind yields the N-W-Y root
recurrently in held-out formula forms above `E[max_Neff]`; failure: root is a deterministic artifact of H1 with no
held-out recurrence beyond chance; the root-forming step scores 0 if it is just na-wa-ya vowel-stripping (circular).

**H3 — gloss "dwell".** success: "dwell" (predefined exact/near/broad/unrelated tiers via WordNet/Semitic lexical
relations) predicts independently observable formula features better than give/dedicate, a neutral placeholder,
random verb glosses, and other N-initial roots; failure: "dwell" is one of many admissible glosses (broad
semantic association), not specifically supported.

**H4 — invocation verb / formula position.** success: leave-one-site-out predicts the verb slot + `*301` behavior
above the formula-template + Markov baselines; NO_POWER if < ~3 eligible sites.

**H5 — morphology (A=1cs / TA=tG stem / I=stem-vowel / *301-WA-JA=root).** success (each morpheme separately):
recurrence across independent residual stems/inscriptions/sites at above-null frequency, AND the parse predicts
held-out forms better than Davis/Thomas/neutral-agglutinative/Markov models, validated on known-script controls
(opaque LB, a known Semitic prefixing language, synthetic agglutinative + synthetic Semitic-root corpora). Report
MORPH_A / MORPH_TA / MORPH_I / MORPH_ROOT / PARADIGM / SEMITIC_MORPHOLOGY separately; one supported slot does NOT
validate the parse.

**H6 — regional paradigm.** success: leave-one-site-out predicts regional forms above baselines; NO_POWER if
insufficient sites.

## Extended hypotheses (H7 ~40 signs · H8 13 LA-only signs · H9 ~408 terms · H10 5 LB signs · H11 logograms ·
H12 reads outside derivation)
- SOURCE_BLOCKED condition (each): the exact artifact (sign table / lexicon / correction list) is not publicly
  obtainable. Per the archive (reports/01) this holds for ALL of H7–H12 as of 2026-07-08 → each returns
  `SOURCE_BLOCKED`, with what was searched recorded, and is NOT collapsed into the H1–H6 verdict. If any artifact
  later becomes public, run the corresponding gate (F/G/H) under this prereg's frozen success statistics.

## Verdict rule (frozen)
`DI_MINO_CORE_VERDICT`: `GRADUATE_EXACT_CLAIM` iff ALL of H1's success clauses + H2/H4/H5 held-out morphology +
H3 beats matched lexicon nulls + survives search multiplicity + no single form/site decisive + not merely a
literature match + positive controls work + end-to-end FP < frozen threshold. `PARTIAL_SUPPORT` iff a named strict
subset survives (e.g. stable `i-*301` stem or invocation-verb role) while /na/-N-W-Y-dwell-Semitic does not.
`REJECT` iff adequate power + fails ≥1 load-bearing clause. `NULL_PUBLISHED` iff indistinguishable from the
fake/root-search null. `NO_POWER` iff the exact test cannot discriminate. Thresholds are FROZEN here; no post-hoc
adjustment.
