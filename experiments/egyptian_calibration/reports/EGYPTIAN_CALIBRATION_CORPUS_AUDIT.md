# EGYPTIAN_CALIBRATION_CORPUS_AUDIT — §IV

The non-Cretan calibration corpus **cannot be populated to standard**. The schema (27 fields), tiers
(A/B/C/X), and inclusion rules are frozen and data-independent, but zero records can be admitted because
no source yields paired (Egyptian group-writing ↔ independently-known foreign form) records with segment
alignment + confidences (§III): Hoch OCR-corrupt at the transliteration layer; Kilani wrong-layer;
Kitchen discussion-only; Muchiki unavailable. Auto-parsing Hoch's corrupt transliteration is forbidden
(it would bake OCR noise into the false-positive calibration the gate depends on). → the load-bearing
input is blocked; downstream fit/control/null/power **cannot be executed to standard**.
