# Wave 2 synthesis — the substitution channel is real (on LB), but Linear A can't use it

This is the campaign's central scientific result, and it is more precise than either predecessor campaign.

## The substitution consonant-held axis is a GENUINE relative-structure signal — on a readable analogue
`C_AUDIT` subjected the C3 opaque-LB result to the EXACT battery that refuted the position channel in WP-A, and
it **survives** (`SUBSTITUTION_CONSONANT_AXIS_VALIDATED`):
- **Independent replication:** 4/4 distinct implementations agree the trustworthy axis is same-consonant
  (AUC 0.643/0.676/0.703/0.692) over same-vowel (~0.55). The two frequency-NORMALIZED scorers (Jaccard 0.703,
  PPMI 0.692) are the STRONGEST — the opposite of a frequency artifact.
- **Frequency-band-disjoint split** (the exact test that collapsed position to 0.481=chance): substitution holds
  at pooled within-band AUC 0.657–0.725; residualizing edge weight on log-frequency leaves AUC 0.634 undiminished.
- **Frequency-matched label null** (position FAILED this, p≈0.130): substitution passes decisively — obs 0.643 vs
  null 0.494, two-sided p=0.0033.
- **Degree-preserving (Maslov–Sneppen) null:** two-sided p=0.0033 at every cutoff; the sister same-vowel axis is
  non-significant (p=0.52) — the detector correctly localizes to the consonant. LOSO-stable; survives Holm.

**This rescues the "internal methods are not value-blind" claim — but via SUBSTITUTION, not position.** Position
(WP-A) was a frequency artifact; substitution is a real, twice-audited, non-frequency corpus-internal
symmetry-breaker on a known script. **Scope caveat (carried):** this validates the DETECTOR on LB (graded with
true values). It does not recover LA structure, does not defeat relabeling-invariance, and earns NO transfer
licence.

## But Linear A does not exhibit the exploitable axis
`C4` (LA substitution graph) + `C5` (stability): the validated detector finds **no value-bearing consonant-held
structure on Linear A** (`CONSONANT_HELD_ANALOGUE = NOT_RECOVERED`):
- **Underpowered:** max long-frame support 3 (vs LB's 98); only 12 sign-edges have ≥2 distinct long frames.
- **Wrong signature:** LA's strongest substitution axis is word-INITIAL (the A-/I- prefix alternation,
  REL_CLASS_01 final-fraction 0.294 < null 0.454) — structurally OPPOSITE to the word-final inflectional slot the
  detector is calibrated for. Audit-1 same-consonant enrichment non-significant (1.94×→1.05×); audit-2 (value-free)
  high-weight edges are LESS word-final than chance (p≥0.889).
- **Fragile:** `C5` = FRAGILE, SEVERE Haghia-Triada dependency (dominant-site holdout collapses the graph),
  segmentation-fragile; honest deflated information-gain **4 bits** (vs 12 if-true).
- What DOES survive is L2/L3: an administrative headword paradigm + productive word-initial affixation
  (A- 11 stems, I- 9, -JA 8). Real structure, no phonetic value.

## Cross-script and anchors close the external routes too
- **WP-F cross-script value transfer: NULL, decomposed.** Of 8 evidence channels, only admin_function/
  distributional is BOTH non-circular AND independently calibratable — and it calibrates to NULL (R@5 0.071 vs
  shuffled-null 0.066; a 4th independent distributional null, concordant with REFUTE_LOTO_FRAGILE). Shape and
  scholarly-correspondence calibrate but are CIRCULAR (LB-identity, capped ≤0.75). 0/59 correspondences rest on a
  primary value source.
- **WP-G anchors: SEED_A = 0.** 115 raw records dedup to 6 method-lineages / 1 value-bearing channel / 5 firm
  toponym equations; expansion yields rich RELATIVE substrate but ZERO new value-bearing anchors; NO
  independently-secure value seed exists (the value substrate is one circular toponym lineage).

## Net (highest claim layer L2/L3; all licences NOT_EARNED)
Internal methods are **not universally value-blind** — substitution genuinely breaks C/V on a readable analogue.
But for Linear A the exploitable axis is **absent** for three concrete, measured reasons: (1) insufficient
long-frame power, (2) a word-initial (not word-final) morphological typology, and (3) no non-circular external
seed to anchor the relative classes to values. The value layer stays closed for LA — not by a theorem, but by a
measured power-and-typology gap plus an empty anchor channel.
