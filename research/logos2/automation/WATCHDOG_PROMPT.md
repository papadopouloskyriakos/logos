Continue LOGOS-2 autonomously from the current repository state (branch
research/logos2-anchor-identifiability, worktree /home/claude-runner/gitlab/n8n/logos-logos2).
Read research/logos2/STATUS.md, the append-only ledger.jsonl tail, automation/AUTOPILOT_STATE.json,
automation/state/experiments.json, and the latest commits BEFORE editing anything.
Check `claude agents --json` for another active worker in this repo family; if one is active, do
nothing and exit. Do not repeat completed work. Preserve failed gates and frozen conclusions
exactly (E201-F1a ALPHABETIC_REPLAY_ONLY_NOT_QUALIFIED_FOR_LINEAR_A; calibration 0/200 CP95
1.49% certifies false-positive behaviour only; E204_BLOCKED_DATA_QUALITY is an extraction-quality
failure, not a metrology result; E207 POSITIONAL_ONLY; E208 de-authorized; all seals unread; no
claim above L0). Continue the next valid node in the experiment DAG (E204R canonical extraction →
E204 metrology if its gate passes; E206 Stage-2 freeze then scoring; E201-F1b ONLY via the
imports/E201_F1B boundary once the external CSA sweep completes; then E212 → E213 → E214 →
final/ deliverables + review zip). Monitor healthy jobs without killing them; commit and push
stable milestones; append the ledger; verify protected assets; never weaken a frozen gate; never
convert blocked data into a negative scientific result; never claim decipherment absent the
global graduation rules; never open a seal whose registered event has not occurred. When an
operation is blocked, fail that branch closed with a machine-readable record and continue every
independent branch, until valid final closure or a hard-stop condition.


## Mission extension (wave 3, 2026-07-10; supersedes nothing, extends the DAG)
The continuation mission now includes, in dependency order: E204R2_residual_canonicalization
(prereg frozen BEFORE parser run; ONE pass; no E204R3 auto-creation) -> E204 metrology ONLY if
E204R2 passes (fraction seal STAYS closed regardless); E206 Stage-2 controlled onomastics
(sources/rules frozen before any external-name inspection; 147 confirmatory / 4 exploratory);
E211-RAG provenance-constrained retrieval (5 separated indexes A-E; PIT cutoffs; Index D =
INTERPRETIVE_CLAIM_ONLY, never support; RAG answers are NEVER evidence; max ordinary graduation
L2); E201-F1b import via the frozen boundary when the external sweep completes; then E212
independence audit (incl. RAG-dependence tracing), E213 prospective seal (hard index cutoff
before sealing), E214 adversarial closure (verifiers incl. RAG separation/citation checks;
banned-wording sweep + 'RAG confirmed'/'retrieved proof'), and the final/ deliverables incl.
RAG_ASSESSMENT.md + review zip. Stop-list additions: unbounded RAG querying; RAG answers as
evidence; indexing unlicensed sources; Index D in confirmatory retrieval; parser repair after
E204R2. Frozen conclusions (incl. E204R_BLOCKED_DATA_QUALITY) remain binding.
