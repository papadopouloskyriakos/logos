# Source-label route CLOSED — pivot to observable-channel grounding

```
NO_HUMAN_SOURCE_LABEL_ROUTE:        status = COMPLETE ; verdict = NO_POWER_BEFORE_MODELING
LINEAR_A_INTERNAL_STRUCTURE_ROUTE:  status = OPEN
SEMANTIC_DECIPHERMENT:              status = NOT_AUTHORIZED
```

The verdict does **not** show Linear A lacks recoverable signal. It shows the proposed **label-based
semantic-role benchmark cannot be evaluated with adequate power** under the no-human constraint (66 non-trivial
REFERENCE_GOLD_A, 2 of 4 load-bearing classes, 19-unit sealed set, edition-independent structural signal 0.026).
A larger model cannot repair a 19-unit sealed set or two missing classes → the original pseudo-script Stage 5
is **not** pursued.

**Pivot (new child branch):** *Observable Administrative Channel Recovery* — avoid semantic gold entirely;
train/eval against directly-observable channels (logogram identity, numeral/quantity, fractions, row/entry
structure, totals, template membership, recurrence). Central falsifiable question: *can opaque word forms
predict independently observable administrative channels when lexical identity, site and series shortcuts are
controlled?* First experiment: masked logogram + accounting-channel recovery under unseen-family, cross-site,
and O2+D8 pseudo-script conditions. Do NOT enlarge the present source-label benchmark unless a new
machine-readable source crosses the predeclared threshold (≥3 load-bearing classes + more independent
morphological families + viable grouped sealed set + simulated O2+D8 power).
