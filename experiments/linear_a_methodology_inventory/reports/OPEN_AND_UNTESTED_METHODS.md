# Open & genuinely-untried methods

What has NOT been tried (distinct from tried-and-null). Descriptive inventory only — not a next-campaign plan.

## Never built (channels that could be non-circular but do not exist in-repo)
- **Palaeographic STROKE corpus cross-script channel.** In the Di Mino F1 decomposition, `stroke_structure` and
  `orientation` were UNPOPULATED — "would in principle be non-circular if a stroke-decomposition corpus were
  acquired." The one shape channel that is not LB-identity-circular has never been built.
- **3D / seal-imaging channel.** No relief/depth/seal-impression evidence ingested; the Anetaki ivory scepter's
  3D evidence is exactly the kind flagged as sealable-delta but unavailable.
- **Genuine bilingual joint decipherment.** Every candidate test is monolingual; the identifiability floor
  (≥10²⁷ twins; 0 exploitable bits) is breakable only by a bilingual or ≥3 independent held-out anchors, neither
  of which exists — so a true bilingual pipeline was never runnable, not merely null.

## Designed / power-gated / blocked before a real run
- **Human-annotated admin-schema gold** (ADMIN-07): DESIGNED_NOT_RUN → the human gate was BLOCKED then
  CLOSED_BY_OWNER; the annotation-dependent schema route was never executed.
- **408-lexicon gate / 40-sign-map gate / LB-correction gate / logogram gate** (Di Mino F/G/H): SOURCE_BLOCKED —
  the exact artifacts are not public; the gates are built-and-frozen but unrun.
- **CSA 600–700-word sufficiency cells**: COMPUTE_BLOCKED — never measured (deposited as endpoint clamps); the
  branch-splitting claim rests on unmeasured cells.

## Prospective (registered, awaiting external evidence)
- **Anetaki II delta seal** (SEAL_5): dated + hashed structural predictions for the unpublished final-edition
  delta of KN Zg 57/58, scoreable only when the editio princeps publishes — the genuine Linear-B-new-tablet test.
- **Newly-excavated inscription arrival**: the source-watch (L1) ranks acquisitions by expected information gain;
  none has arrived to trigger a prospective run.

## Method variants not attempted at all
- Joint sign-value × morphology × formula-grammar inference *conditioned on a confirmed bilingual anchor* (the
  agnostic search was run unconditioned → underdetermined).
- Cross-script transfer using the *substitution consonant-axis* (the only validated internal channel) as the
  bridge feature rather than raw distribution — proposed by the lineage logic but never implemented.
- Active-learning / adaptive corpus-acquisition targeting the signs with highest equivalence-class-reduction value.
