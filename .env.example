# logos comparison-layer DB (MySQL/MariaDB). Copy to .env and fill in.
# DB-free unit tests do NOT need this; only the integration tests + the live gate do.
LOGOS_DB_HOST=127.0.0.1
LOGOS_DB_PORT=3306
LOGOS_DB_USER=logos
LOGOS_DB_PASS=changeme
LOGOS_DB_NAME=logos
