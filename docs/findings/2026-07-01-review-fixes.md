# Finding 2026-07-01 — pre-submission code fixes v2: verify-before-fixing, full e2e re-run, old→new diff

This records the pre-submission CODE pass (P0–P5). The governing rule was **verify each claimed bug
against the actual code before touching it** (the external review was run against an older commit),
then **fix the real ones fail-closed, re-run every affected probe end-to-end from scratch, and rebuild
every number in the docs from the regenerated artifacts** — never patch prose with assumptions
(invariant 12). Every artifact below was regenerated by running the pipeline; the diff table is
computed from `runtime/*.json`, `results/*.json`, `scripts/cross_script/results_ab.json`, and
`corpus/bronze/palaeo/results_palaeo.json`, comparing a pre-change snapshot to the post-change re-run.

## STEP 0 — claims that were STALE (verified against code, left untouched)

| review claim | verdict | evidence in current code |
|---|---|---|
| "n_comparisons isn't Bonferroni-corrected" | **STALE** | `verdict.py` already sets `n_cmp = max(1, len({eps}))` and deflates by it — correct at the audited commit. |
| "S_morph claims morphology with no power" | **STALE** | `verdict.py` `morph_gold_standard_when_powered` already returns `True` (non-blocking) when `has_power` is False — the no-power escape is already correct. |

These were reported as stale, not fixed; no code change.

## REAL fixes (each fail-closed, each with a regression test)

### P0 — three gate bugs (`scripts/verdict.py`), verified present
1. **MDL clause removed from the gate.** `k_le_u_floor` previously sat inside the AND with a
   free-parameter default, so it could pass degenerate input. Removed as a *clause*; `u_floor` is now
   **reported-only** (NaN-safe) next to every claim (invariant 7 preserved). Regression:
   `test_mdl_clause_removed_u_floor_reported_only`, and `test_gate_clauses_fail_closed_on_degenerate_input`
   asserts `"k_le_u_floor" not in clauses`.
2. **Pre-registered `generalizes_to_virgin` threshold.** The virgin-generalization clause now requires
   a pre-committed `virgin_threshold` AND ≥ `virgin_min_signs` (default 2) distinct supported L_virgin
   signs; it **fails closed** when either is absent (no threshold ⇒ no graduation). Regression:
   `test_litindex_virgin_support_feeds_the_e_gate_via_grade_row`.
3. **Order-statistic bar promoted, DSR demoted.** The operative deflation clause is now
   `beats_order_stat_bar` (observed must exceed E[max] of the null over `n_trials`), which needs no
   normal approximation and fails closed on a zero-variance null. DSR is still computed/reported.
   `n_eff`→`n_trials` (and `n_eff_source`→`n_trials_source`) throughout. Regression: degenerate constant
   null ⇒ `beats_order_stat_bar` False.

### P1 — null-simulation robustness
4. **`n_fake` default 16 → 300** (`run_canary.py` `run()` + CLI), so the corrected-margin bar (Cornish–
   Fisher on the L_fake skew/kurtosis) is estimated from enough instances to resolve higher moments;
   `nulls.null_distribution` draws `n_packard=n_random=n_within` 32 → 100.
5. **Exact rank p-values** in `morphostat.s_morph` (`n_null` 64 → 1000): the decision is now
   `p_rank = (#{null ≥ score}+1)/(B+1)` vs a Šidák/normal α, replacing the normal-approx z-test; adds
   `p_rank`, `p_rank_se`, `alpha`, `n_null`. Note: `s_morph` is called **only** by `verdict.py` (the
   gate's morph clause) — not by the `morphology.py` probe — so this hardens gate decisions, not the
   published morphology numbers.

### P2 — cross-script image result: font-swap control + honest relabel
6. **Font-swap confound control** (`scripts/palaeo/font_control.py`, artifact
   `results/palaeo/font_control.json`; `render_glyph` now takes a `font_path`). The baseline draws BOTH
   scripts in one face (Aegean); the control re-renders Linear A in **Noto Sans Linear A** and Linear B
   in **Noto Sans Linear B** — two independent single-script designers (cmap-verified: NotoA has 0
   Linear B glyphs, NotoB 0 Linear A). **Result: the font-artifact hypothesis is REFUTED** — recovery
   survives and slightly strengthens (direct-NN 0.367 → 0.416; Procrustes 0.156 → 0.195; chance 0.0135;
   ~113% retained). The image leg is nonetheless **relabelled a "shape-genealogy engineering
   demonstration, not an archaeological positive"** for the deeper, un-controllable reason the font swap
   cannot touch: the A↔B anchor *values* were assigned by epigraphers on the basis of sign shape (Linear
   B descends graphically from Linear A), so recovering that value map from glyph images is largely
   circular. I-JEPA is kept out of the main claims (over-parameterized for ~90 glyphs). Regression:
   `tests/test_font_control.py` (3 tests, cmap single-script property + mechanical verdict + determinism).
   The preprint's "one modest offensive positive" framing is withdrawn (§7.3, §8, §9): **the study
   reports no offensive positive.**

### P3 — full ε-grid at run time
7. `run_canary.run()` and the CLI **already default** to the full grid `(0.15, 0.20, 0.25, 0.30)` and
   `n_fake=300`, so `n_comparisons` reflects the true multi-ε search. Verified, no change needed.

### Reproducibility hardening (surfaced by the e2e re-run)
- **Morfessor determinism** (`morphology.py`): the segmenter carried a `seed` that was never applied —
  `train_batch` shuffles via stdlib `random`, so the descriptive boundary-recovery / morpheme-inventory
  numbers drifted ±0.01–0.02 across identical re-runs. Now seeded before training; two runs are now
  bit-identical. The **headline** morphology falsification never depended on this and always reproduced.

## P4 — full e2e re-run: old → new diff table

Every deterministic probe was re-run from scratch (seeded) and diffed against a pre-change snapshot.
Run local (20-core), single-threaded BLAS (`OMP_NUM_THREADS=1` — the canary is Python-bound, not BLAS;
the H100 was declined: the only GPU job, I-JEPA, is a tiny ~90-image model that finishes in seconds on
CPU, and the bottleneck is single-threaded string-matching a GPU cannot accelerate).

| probe / artifact | old | new | status |
|---|---|---|---|
| morphology headline (`runtime/morphology-real.json`): n_confirmed / real / lfake / power | 4 / 0.250 / 0.375 / False | 4 / 0.250 / 0.375 / False | **reproduced exactly** |
| morphology descriptive (morfessor F1, inventory counts) | ±0.01–0.02 drift per run | now **bit-stable** (seed fix) | fixed → reproducible |
| stratified (`morphology-stratified.json`): `has_validated_morphology` | False | False | **reproduced** |
| phonology (`phono-distributional.json`): bridge_detected / n_testable | False / 50 | False / 50 | **identical** |
| metrology (`metrology-real.json`): held-out fraction balance / p / separated | 0.0 / 1.0 / False | 0.0 / 1.0 / False | **JSON identical** |
| cross-script sequence (`results_ab.json`): all 6 methods vs chance | at chance (≤0.021) | at chance (≤0.021) | **reproduced exactly** |
| cross-script IMAGE (`results_palaeo.json`): classical direct-NN / Procrustes | 0.410 / 0.185 | 0.4095 / 0.1845 | **reproduced exactly** |
| font-swap control (`font_control.json`): NEW — Aegean → Noto cross-font direct-NN | (did not exist) | 0.367 → 0.416 (font confound REFUTED) | **new artifact** |
| gate clause set (`verdict.py`): operative clauses | k_le_u_floor, dsr_ge_0_95, … | beats_order_stat_bar, generalizes_to_virgin, … | **structural change; verified by 359-test suite** |
| L_fake canary (`results/lfake_canary.json`): real `S_lex` per ε | 0.477/0.550/0.759/0.761 | 0.477/0.550/0.759/0.761 | **reproduced** (n_fake-independent) |
| L_fake floor p95 per ε | 0.012/0.031/0.279/0.279 (n=16) | 0.020/0.074/0.384/0.384 (n=300) | thicker, better-resolved tail (§ canary) |
| L_fake canary verdict | CANARY_HOLDS | CANARY_HOLDS (vs corrected bar) | **conclusion holds** |

**Interpretation of the diff.** The re-run *validates* the published numbers: every probe reproduces
exactly (morphology headline, palaeo 0.410, cross-script sequence null, phono, metrology). The code
fixes are structural — a stricter/fail-closed clause set and thicker nulls — and because **nothing has
graduated** (no persisted verdict carries gate clauses), the P0 gate change moves **no published
GRADUATE/REJECT number**; it is verified instead by the full 359-test suite. The two genuinely new
results are the font-swap control (which *refutes* a reviewer's confound rather than confirming it) and
the n_fake=300 canary bar. The morfessor drift is now eliminated.

## Full test suite (e2e, from scratch)

`pytest -q`: **359 passed, 4 xfailed** (80s), including the P0 fail-closed gate tests, the P1 exact-rank
tests, and the new `tests/test_font_control.py`. Warnings are the already-guarded skimage Hu-moment NaN.

## L_fake canary — n_fake=300 re-run (regenerated, `results/lfake_canary.json`)

Re-run from scratch at n_fake=300 with the full ETCBC/bhsa reject set. `root_template_TV = 0.33`
and `residual_real_collision_rate = 0.0` (both match the preprint's calibration claims). The
positive control (real Ugaritic↔Hebrew `S_lex`) is `n_fake`-independent and reproduces the prior
run exactly; the L_fake floor is now estimated from 300 instances, so the tail (p95) is higher and
better-resolved than the 16-instance run, and the operative bar is the corrected-margin
(Cornish–Fisher skew/kurtosis + Bonferroni-over-ε) value:

| ε | REAL cognate `S_lex` | L_fake p95 (n=16, old) | L_fake p95 (n=300) | corrected-margin bar (n=300) | verdict |
|---|---|---|---|---|---|
| 0.15 | 0.477 | 0.012 | 0.020 | 0.023 | CANARY_HOLDS |
| 0.20 | 0.550 | 0.031 | 0.074 | 0.079 | CANARY_HOLDS |
| 0.25 | 0.759 | 0.279 | 0.384 | 0.389 | CANARY_HOLDS |
| 0.30 | 0.761 | 0.279 | 0.384 | 0.390 | CANARY_HOLDS |

**Conclusion unchanged and now on a properly-estimated floor:** real cognates clear the
corrected-margin bar at every ε (margins 0.45 / 0.47 / 0.37 / 0.37); `CANARY_HOLDS_REAL_CLEARS_FLOOR`
throughout. The thicker floor is the point of P1.4 — the 16-instance p95 understated the L_fake tail.

**Runtime engineering (surfaced here).** At n_fake=300 the canary is dominated by `s_lex`
(Ugaritic↔Hebrew edit-distance cognate scoring): ~1,500 scorings × ~1.9 s ≈ 45 min serial. This is
a CPU string-matching workload — **not** a GPU one (branchy edit distance, not dense linear
algebra), so an H100 gives no speedup; the fix is CPU process-level fan-out. `run_canary` now scores
the fake-lexicon and null passes through a `ProcessPoolExecutor` (`_parallel_slex`), verified
**bit-identical** to the serial path (fake-recalls and all three null generators), cutting the
n_fake=300 run to **~5.5 min** on the 20-core box.

## P5 — pre-registration anchor

A **signed** git tag anchors the reviewed methodology + code state, tamper-evidently, distinct from
the post-hoc result commits it caps:

- Tag: `review-fixes-2026-07-01` (SSH-signed, ED25519; verifies **Good signature** for
  `svc-claude@nuclearlighters.net`).
- Tag object: `859245c5633d7604069fa4311d82ae662e17d146`
- Anchored commit: `b05f1e5925cf24687d4deb6496288b4fe314d438`
- Post-hoc result commits capped by the tag: `fa51f4c` (code + docs) and `b05f1e5` (regenerated
  n_fake=300 canary artifact + diff table).

Verify with `git tag -v review-fixes-2026-07-01`. The signature over the anchored tree is the prereg
receipt: the gate/canary/null methodology cannot be silently altered after the fact without breaking it.

## Addendum 2026-07-01 — DĀMOS ingested + two acquired papers folded in

**DĀMOS was harvested but not ingested** — a stale "data-blocked until DĀMOS" claim in the preprint.
Fixed: `data.load_b_damos()` parses the harvested `items.jsonl` (5,840 tablets) into **13,562
Linear-B wordforms** (≈15× the 918-wordform cognate file; 89 sign-values; 56 anchors), filtering
logograms/metrograms/numerals to keep only syllabic wordforms. `run_ab.py --b-source damos` re-runs
the cross-script alignment on it. **Result: still at chance** (CCA 0.025 vs chance 0.011,
`clearly_above_chance = False`; positive control 0.947) → the block is an *absent distributional
signal*, not corpus volume. This **refutes** "data-blocked until DĀMOS," so §7.2/§7.3/§8 of the
preprint and the AB-alignment finding were corrected from a promissory note to a tested null.
Artifact: `scripts/cross_script/results_ab_damos.json`. Cross-script tests: 70 passed.

**Two blocking papers acquired (operator) and audited against the primary sources:**
- **Ferrara & Tamburini 2022** (*Lingue e Linguaggio* XXI.2:239–259) — verified. Its §3.5 reviews the
  exact cognate-matching lineage logos uses (Snyder 2010 Ugaritic↔Hebrew — the same benchmark the
  canary calibrates against; Berg-Kirkpatrick 2011; Luo 2019/2021) and classes Linear A as Gelb-Whiting
  Type III ("validation altogether impossible"), **yet never treats false-positive / multiplicity
  control** — so logos's "sixteenth challenge" gap holds against *both* field surveys, not just
  Braović. §2 strengthened accordingly.
- **Fuls 2015** (*Historische Sprachforschung* 128(1):42–58) — verified §6.5 (p.57): the 3.3-sign
  figure is the mean over "554 complete sign sequences, duplicates reduced to one" (GORILA/Godart &
  Olivier 1985; Timm 2008) — distinct word *types*, confirming the preprint's token-vs-type "different
  denominator" reconciliation. Added: Fuls infers Minoan is *polysynthetic* from word length; logos's
  morphology null (affixation bigram-reproducible, no power) does **not** corroborate that — flagged as
  an honest tension. §6 tightened to cite the primary + the typology tension.
