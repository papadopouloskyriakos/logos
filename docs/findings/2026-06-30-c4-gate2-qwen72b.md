# Finding 2026-06-30 — C.4 gate-2 on qwen2.5:72b: per-word regurgitation table (the rate stays confounded; the table is real)

> **AUDIT CORRECTION (external red-team, post-`691fe09`) — read first.**
> 1. **`KU-RO` is a confounded witness; split it.** Recomputed from the persisted artifact, qwen's 14/40
>    `KU-RO` hits are **8/40 via the Semitic root `kull`** (Gordon memorization) and **7/40 via the meaning
>    `total`/`sum`** — and `total` is confirmed by tablet *arithmetic* and is general knowledge (every popular
>    LA description, Wikipedia). So `KU-RO`=`total` is **excluded** as a contamination witness. Lead the clean
>    signal with **`JA-NE`=yn=wine (14/40)** and **`A-SA-SA-RA-ME`=Asherah (2/40)** (no innocent route);
>    `KU-RO`=`kull` (8/40) is the Semitic-only portion; **downweight `KU-RO`**.
> 2. **Narrower wording.** What C.4 detects is **published-reading reproduction weighted by web-prevalence**,
>    NOT specifically "Semitic-literature contamination."
> 3. **Headline.** The strongest result is **the discipline catching its own confounds twice**; this table is a
>    *promising, confound-laden lead pending the L_virgin test*, not "the headline empirical result."
>
> The numbers below are unchanged and correct; the *interpretation* is the corrected one above.

Re-ran the §C.4 ablation with the **gate-2 cognate-level** machinery (litindex now holds 6 verified
Semitic readings; cognates reconciled to a common consonant-skeleton vocabulary; litindex-word probe
forms prepended every seed). `qwen2.5:72b` on a rented vast.ai H100 (40 seeds × 40 forms + 7 probes;
~$0.93, instance destroyed). The **cognate_contamination_rate is confounded by the model-free baseline's
representational ceiling** (verifier-proven; flagged `rate_confounded=True`) so it is NOT reported as a
contamination measure. The honest deliverable is the **per-word reproduction table**.

## Per-word reproduction (over 40 seeds)

| Linear A word | published reading | **qwen2.5:72b** | model-free arm |
|---|---|---|---|
| `SU-PU` | sp (Gordon) | 19/40 (48%) | **40/40** |
| `KU-RO` | kull / "total" (Gordon) | **14/40 (35%)** | 0/40 |
| `JA-NE` | yn / "wine" (Gordon) | **14/40 (35%)** | 0/40 |
| `A-SA-SA-RA-ME` | "Asherah" (Best 1981) | 2/40 (5%) | 0/40 |
| `KA-RO-PA` | krpn (Gordon) | 0/40 | 0/40 |
| `SU-PA-RA` | spl (Gordon) | 0/40 | 0/40 |
| `KI-RO` | "deficit" (Semitic) | 0/40 | 0/40 |

## What this says (honestly)

1. **qwen reproduces specific published Semitic readings, SELECTIVELY.** It reproduces the *famous*
   ones — `KU-RO`=kull/total (in every Linear A overview) and Gordon's `JA-NE`=wine — at ~35% of
   seeds, and Best's `A-SA-SA-RA-ME`=Asherah occasionally (5%). It **never** reproduces the obscurer
   vessel equations (`KA-RO-PA`=krpn, `SU-PA-RA`=spl) or a Semitic `KI-RO`. The gradient (well-known
   readings reproduced, obscure ones not) is consistent with **memorization of well-published
   scholarship** — the contamination C.4 is built to catch, now at per-word resolution rather than one
   opaque number.
2. **The model-free arm reaches ONLY `SU-PU` (40/40), nothing else (0/40)** — confirming empirically
   the verifier's structural-ceiling finding: a syllabic edit-distance baseline can express a published
   reading only when the form's syllabic skeleton coincidentally equals it (`sp`=`sp`). `SU-PU` is
   therefore NOT distinctive evidence of laundering (a dumb metric gets it too); the distinctive
   regurgitation signal is `KU-RO` and `JA-NE`, which qwen reproduces and the baseline cannot.
3. **The confounded rate** came out mean 0.646 (not a hard 1.0, because `SU-PU` is a genuine shared
   hit that lands in the denominator's "also reproduced by mechanical" set). It is still confounded and
   NOT a contamination measure — recorded only for completeness.

## Bottom line + next steps

The per-word table is a real, honest measurement: **qwen2.5:72b demonstrably reproduces Gordon's
`KU-RO`=kull and `JA-NE`=yn (and occasionally Best's Asherah) — published readings a model-free metric
does not independently reach.** That is concrete evidence of literature contamination in a frontier-
scale open model, reported at the resolution it actually supports.

A clean *contamination rate* (and a clean discovery test) needs the two things the confound exposed:
1. **A stronger, Gordon-free model-free baseline** — the phonetic JEPA. NOW UNBLOCKED: the DĀMOS Linear
   B corpus (~5,932 docs) is being harvested (`scripts/fetch_damos.py`); a JEPA trained on Linear B
   (no Gordon) could express reinterpreted readings fairly.
2. **The `L_virgin` generalization test (§C.2)** — does qwen's reasoning hold on signs with NO published
   value (which it cannot have memorized)? That disentangles reasoning from memorization directly.

Provenance: `qwen2.5:72b` (Ollama q4) on vast.ai H100 SXM 80 GB (ephemeral). Raw result
`runtime/ablation/gate2-qwen72b.json` (gitignored). Harness `9cd6d04`; honesty fixes in the same.
