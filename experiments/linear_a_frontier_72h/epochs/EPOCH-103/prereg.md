# EPOCH-103 preregistration — A- prefixation strong-lead adjudication (§6)

**Reason:** ADJUDICATE_STRONG_LEAD. **Frontier:** F4 (held-out robustness of the campaign's strongest LA positive).
**Layer ceiling:** L2/L3 (positional SLOT only; NO phonetic value / sound / meaning / language). **Licence effect:** none expected.

## Lead frozen for adjudication
A- is a productive, corpus-wide, word-initial positional prefix SLOT in Linear A administrative words (len>=2 signs).
Established under a SINGLE segmentation scheme by E022 (global adaptive-null PERMUTE p=.0002, best-of-72 maxT p=.0002,
sole survivor), E023 (cross-site 9/10 sites + LOO), E024 (support 5/6 + chronological 4/4, multi-axis).

## The indispensable missing element (why this epoch is authorized under the <=120 ceiling)
E022-E024 all used ONE segmentation (editor `word` tokens). §6 requires re-running under >=2 sign inventories or
segmentation schemes + leave-one-method-out + a matched adaptive null. This epoch supplies exactly that and issues a
mechanical verdict.

## Committed design (frozen before running)
Three independent segmentation schemes from the same silver stream:
- **A_editor** — editor `word` tokens (reproduces E022-E024).
- **B_divider_strict** — units bounded only by physical div/nl/num/other; editorially-split adjacent words MERGED.
- **C_numeral_anchored** — only the sign-run immediately before a numeral (ledger entry heads); a-priori lower A-rate.

For each scheme: within-word permutation null (M=5000, per-word uniform sign-order) for A-initial incidence AND a
correlation-aware best-of-universe maxT deflation over all signs with >=5 initial occurrences (shared draws). Report
A's single-permute p, A's maxT p, A's rank, and the full p<=.01 survivor set.

Positive control: plant a synthetic initial sign 'ZZ' at 15% (power) + a mid-frequency sign on order-scrambled words
(calibration). LOMO: confirm the conclusion is invariant to dropping any one of E022/E023/E024.

## Committed prediction
Modal: A- remains the TOP maxT survivor at p<=.01 under >=2/3 schemes -> REPLICATED_RELATIVE_CONSTRAINT.
Alt: significant under only 1/3 -> CONDITIONAL_SIGNAL_ONLY (segmentation-dependent). PC-fail -> NO_POWER.

## Verdict rule (mechanical)
REPLICATED_RELATIVE_CONSTRAINT iff PC power+cal clean AND A- is top survivor at maxT p<=.01 under >=2/3 schemes.
CONDITIONAL_SIGNAL_ONLY iff exactly 1/3. GENERIC_UNDER_NULL iff 0/3. NO_POWER iff PC fails.

## Forbidden
No phonetic value, sound, translation, or language-family inference. The finding, if replicated, is a RELATIVE
positional-slot constraint (L2/L3) only. Any sole-survivor discrepancy vs E022 is recorded append-only (Art. XVII).
