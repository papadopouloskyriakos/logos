EPOCH-033 data artifacts — LEDGER ENTRY-WORD vs NON-ENTRY-WORD STRUCTURAL CONTRAST

Source corpus: corpus/silver/inscriptions_structured.json (1341 inscriptions)
Generated by: epochs/EPOCH-033/machinery.py (run() -> self-check stdout)

Files:
  global_summary.json     — global class counts, length & A-rate contrasts + perm p
  positive_control.json   — LB PC: detect-planted + false-positive
  cross_site.json         — per-site tests (8 testable sites) + LOO (Haghia Triada)
  word_records.jsonl      — one line per word token: {site,is_entry,len,a_init} (3147 lines)

Definitions (FROZEN):
  ENTRY-word    = 'word' token whose IMMEDIATELY FOLLOWING stream token is 'num'
  NON-ENTRY word= 'word' token whose following token is NOT 'num' (or last)
  LENGTH        = number of signs in the word token
  A-INITIAL     = first sign == 'A' (over >=1-sign words)
  CONTRAST      = entry minus non-entry
  NULL          = label permutation (permute entry/non-entry labels across the same word-token
                  set, preserving class counts); >=2000 draws; two-sided p
