# OBSERVABLE_CHANNEL_READY — gate verdict

Mechanical verdict on the Observable Administrative Channel Recovery programme (Constitution v2.0;
`PROJECT_CHARTER.md` gate). Synthesizes Experiments 1–4. Append-only (Art. XVII).

## The central falsifiable question

> *Can opaque word forms predict independently-observable administrative channels when lexical identity,
> site shortcuts, and document-series shortcuts are controlled?*

**Answer: NO.** Verdict: **`REFUTED`** at the load-bearing layer (L3 functional word-context).

## Evidence

| exp | channel | claim layer | word-context (M_sign) under A12 | verdict |
|---|---|---|---|---|
| 1 | commodity (logogram) | L3 | 0.182 vs 0.175 freq / 0.63 series; neg. cross-site | NEGATIVE |
| 2 | quantity (presence + magnitude) | L3 | 0.651 **< 0.68** baseline; magnitude **< null** | NEGATIVE |
| 3 | accounting-closure | L2 | (numeral-only) closure 0.071 > null 0.049; exact reconstruction where present | L2 positive |
| 4 | document-template | L2 | (structure-only) 0.508 > 0.398 held-out; single-direction cross-site | L2 positive |

## Gate evaluation (charter conditions)

| condition | met? |
|---|---|
| word-context beats the strongest frequency/series/layout baseline | **NO** (both L3 channels below/at baseline) |
| success on ≥2 distinct channels **at L3** (functional) | **NO** (0 of 2) |
| under unseen lexical-family (A12) | yes (enforced; the negatives are A12-clean) |
| ≥1 KN↔non-KN direction | word-context: **NO** (negative/at-baseline cross-site) |
| survives O2+D8 pseudo-script | not reached (nothing L3 survived to stress-test) |
| no dependence on site/series/lexical identity | the only positive signal (L2) IS the site/series/layout structure |

**`OBSERVABLE_CHANNEL_READY = NO`** for the functional decipherment goal.

## What the programme delivered

1. **A rigorous, well-powered L3 negative** (the primary result). Across two dense, balanced channels
   (457 + 1,593 A12 units — not underpower), opaque word forms carry **no transferable functional signal**
   once document structure is controlled; they overfit and transfer negatively. This closes the
   word-form→function route as a bounded negative — consistent with the label-route `NO_POWER` and the
   Egyptian-anchor `TRIVIAL_RECOVERY`. *A negative verdict is a successful scientific result (Preamble).*
2. **A characterized L2 structural channel** (secondary, `SUPPORTED` at L2): documents are internally
   templated (0.508 vs 0.398 held-out) and exhibit sparse-but-exact accounting closure (100% masked-line
   reconstruction where present). This is real but is the **already-known L2 layer** (segmentation/structure
   in the paper), and it is exactly the site/series/layout *convenience* the gate requires removing — so it
   does **not** earn a functional licence.

## Licences (Art. XV) — both resolved (Exp 5–6 now run)

- **FUNCTIONAL_TRANSFER_LICENSE:** `NOT_EARNED` — the L3 word-context question is `REFUTED`.
- **STRUCTURAL_TRANSFER_LICENSE:** `NOT_EARNED` — Exp 5–6 ran and the gate does **not** clear:
  (1) **site-dependent** (Exp 5: type-distribution TVD 0.28, gap null-p 0.0 → `SITE_DEPENDENT`);
  (2) the second channel (closure) is **D8-fragile** (Exp 6: decays to the null at 35% retention);
  (3) the one robust signal (template regularity) is **sign-agnostic** (Exp 6: O2-identical 0.497=0.497) —
  it carries no sign/lexical/functional content and is the already-known L2 document grammar. See
  `reports/EXPERIMENT5-6_INVARIANCE_PSEUDOSCRIPT.md`.

## Disposition — programme `COMPLETE`

Both gates resolved as **well-powered negatives**: L3 functional `REFUTED`, L2 structural-transfer
`NOT_EARNED`. The observable-channel route is closed. No Linear A output authorized (SEMANTIC+
`NOT_AUTHORIZED`; no licence earned). A complete negative that maps what the corpus supports = a successful
scientific result (Preamble). The document-structure signal that exists is real but content-free positional
grammar — the paper's already-established L2 layer.
