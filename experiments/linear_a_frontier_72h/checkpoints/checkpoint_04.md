# Checkpoint 04 — epochs 12–16 (clock ~62h; 16/18 banked)

- **E012 = ALLOGRAPH_STRUCTURE_CONFOUNDED** (F6): the doc→site allograph channel is fool's-gold — beaten
  by a pure rendering-nuisance embedding (0.331 > 0.280); per-sign aggregate spread is individually robust
  (within < cross-site, p 1e-4) but the doc-level channel carries a tracer/scribe confound. Decisive
  de-confound = photograph-level re-extraction (queued).
- **E013 = DECOMPOSITION_NEUTRAL** (F2/F5): a component-decomposition coverage instrument recovers 764/1009
  extraction failures; dark-sign extraction hits the 69/69 ceiling and is robust 21→24/69 (ATTESTATION is
  the binding constraint, not extraction). Component-level structure is a **stronger** cross-script channel:
  agg MRR +44% → 0.247 (p 5e-5).
- **E014 = REGISTER_STRATA_NO_POWER** (F7): register/scribal stratification adds no separable phonotactic
  signal at LA scale.
- **E016 = SBI_MATCHES** (F9, calibration-only): a trained neural simulation-based-inference posterior
  **ties but never beats** raw-cosine spectral clustering at LA scale — **no support for adopting the
  architecture**; independently reproduces E003's anchor-geometry bottleneck under a disjoint estimator;
  large sim-to-real gap (ARI 0.318→0.071); LA never loaded (L2, no licence). (Recovered on Sonnet after a
  Fable-5 credit compute block — no science lost.)
- **E015 = PC_LB_GATE_VACUOUS / A-_PREFIX_ROBUST_REPORTED / NO_NEW_LA_AFFIX** (F4): segmentation-marginalized
  morphology. The preregistered PC-LB gate came back `MACHINERY_UNINFORMATIVE` (empty LB-TEST target at the
  30% split), so the improvement direction is **characterized, not licensed**. But where truth is known the
  result is decisive: synthetic control **GRACEFUL** (ΔF1 +0.21/+0.42/+0.69 as hapax load rises, 5/5 seeds),
  and a well-powered posthoc LB target shows marg recovery F1 0.80 vs frozen 0.00. On LA itself: **A-
  prefixation is robust under marginalization and marginalization sharpens it** (marg p 0.0005 vs frozen
  0.002; reported, not gate-validated), and **0/71 new-affix candidates survive Holm**. (Completed by the
  main loop from on-disk scripts after the same Fable-5 block + a placeholder return.)

- **Compute-block note (Art. — not an early-stop condition):** the E015/E016 workflow first returned
  COMPUTE_BLOCKED (Fable-5 usage credits exhausted). Per the campaign rules this is neither a scientific
  null nor a stop condition; both epochs were re-run on Sonnet 5 (model pinned in the workflow script) and
  banked. No filler, no fabricated data.

- **Tally @16/18:** calibrated channels 1 (stroke, E009) + 1 stronger cross-script channel (component
  decomposition, E013); positives — A- robust-under-marginalization (E015), decomposition cross-script lift
  (E013); formal negatives/nulls — E010 seeded-bridge exhaustion, E011/E014 NO_POWER, E016 no-architecture-
  gain, E012 confound-exposed; power surfaces 2 (E003 113-cell, E010 capacity) — **§12 needs a 3rd**;
  adaptive-null families heavy per-epoch but **§12 needs 3 full dedicated families (≥200 realizations)**;
  seals 3 frozen.

- **Next (→18 and beyond):** (1) **photograph-level de-confound** epoch for F6 (decisive for E012's tracer
  confound; SigLA serves photos) → 17/18; (2) **dedicated adaptive-null family** epoch (≥200 realizations)
  toward §12's requirement → 18/18 (min reached — clock still governs finalization); (3) **third power
  surface** (§12); (4) LIB-internal geography (E014 successor); (5) eventual **frontier exhaustion map**
  (§12 mandatory). Clock: `finalization_authorized=false`, ~62h + reaching all §12 requirements remain.
