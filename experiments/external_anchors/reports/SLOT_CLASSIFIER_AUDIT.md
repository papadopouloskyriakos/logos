# Slot-classifier audit — `slot-rules-v1-2026-07-06`

Generated by `build_manifest.py` from the pinned read-only corpus. Manifest gold sha256
`dc9653a91d7ba14dcb762389…` (full hash in `data/manifests/slot_candidate_manifest.sha256`).

## Counts by class × tier (1165 candidates)

| class | A | B | C | X | total |
|---|---:|---:|---:|---:|---:|
| TOPONYM_LIKE | — | **11** | 33 | — | 44 |
| PERSON_NAME_LIKE | — | — | 359 | — | 359 |
| COMMODITY | 27 | — | — | — | 27 |
| TRANSACTION_TERM | 3 | — | 4 | — | 7 |
| RITUAL_FORMULA | — | 10 | 233 | — | 243 |
| UNKNOWN | — | — | 115 | 370 | 485 |
| **total** | 30 | 21 | 744 | 370 | 1165 |

## Reading
- **The confirmatory toponym targets are the 11 tier-B `TOPONYM_LIKE` forms** (entry-leading, ≥2
  sites AND ≥3 documents) — plus 33 tier-C for sensitivity. This is the internal target set the
  Kom el-Hetan Egyptian toponyms (Knossos/Amnisos/Lyktos) will be tested against in E3 — *held out by
  site*, never chosen for resemblance.
- COMMODITY (27, tier A) = logogram forms; TRANSACTION_TERM (3, tier A) = KU-RO/KI-RO/PO-TO-KU-RO.
  These are the high-certainty semantic anchors for the RQ1/E5 channel.
- PERSON_NAME_LIKE (359, all tier C) is large and weak by design — most are hapax entry-leaders;
  they are **not** confirmatory targets (the personal-name channel is gated and runs last), and given
  the EA5647 finding (Cretan personal names in Egypt read as *Greek*) this channel is de-prioritized.
- 370 tier-X (damaged/uncertain/degenerate) are excluded from confirmatory use up front.

## Leakage protections verified (11/11 tests pass)
- stdlib-only imports; opens only the two pinned corpus files (drift-checked at load);
- no `anchors/`, `calibration/`, or `candidate_languages` path in the classifier code;
- every manifest row `leakage_exclusion_status = internal_only_clean`; no `phonetic`/`greek` token in
  any row;
- `build_manifest.py` refuses to run if a matcher module exists → the manifest is provably frozen
  **before** any external matching.

## Unresolved transcription issues (carried forward)
- **Damage/uncertainty is retained but coarse:** the tier-X exclusion keys on bracket/`*` markers in
  the *word-form string*; per-sign confidence and alternative readings are **not** yet propagated
  (`alternative_transcriptions = []`). The power/confirmatory passes must re-run over plausible
  transcription variants (Task D sensitivity), per the representation-audit finding in the main repo.
- **TOPONYM_LIKE vs PERSON_NAME_LIKE is a recurrence heuristic**, not a proof; the tier-B/C split and
  the site/doc thresholds are the levers a sensitivity analysis must vary.
- **TITLE_OR_OFFICE / INSTITUTION are intentionally empty** — no internal signal reliably separates
  them; forcing them would manufacture structure. They remain UNKNOWN.
