# Scope freeze — 2026-06-30 (external audit P3)

An external red-team reviewed the post-`691fe09` state and issued a **scope freeze**: the contribution
is fully visible; stop building machinery and start drafting. Recorded here as the authoritative plan
adjustment (supersedes the open-ended build queue in `OPERATOR-AFK.md`).

## The contribution (already in hand — do NOT keep adding to it)
- The **decontamination / discipline methodology**: L_fake canary + L_known/L_virgin + LLM-ablation +
  the integrated graduation gate (pre-registration, held-out, permutation null, MDL, selective inference).
- The **cleaned sign ontology** (V=92) + the **information floor done honestly** (toy-model precondition
  diagnostic, identifiability withdrawn).
- The **A↔B asymmetry**: image *shape* transfers (~24–30× chance) but the *distribution* does not
  (Track B at chance) — an honest, interesting result.
- The **contamination lead**: a frontier model reproduces web-prevalent published readings a model-free
  method cannot reach (a *lead pending L_virgin*, not a settled claim).

## The only remaining experiments (then draft)
1. **#4 L_virgin generalization test** — signs with no published value cannot be memorized; the clean
   memorization-vs-reasoning cut. Grade from the persisted per-sign artifact + a chance baseline, never
   from the model's prose. *(In progress.)*
2. **#5 Gordon-free phonetic baseline on harvested DĀMOS** — a Linear-B-trained, Gordon-free model-free
   baseline that makes a *clean* contamination rate possible (replaces the edit-distance baseline whose
   representational ceiling confounded gate-2). Until it exists, report the per-word table, **not a rate**.

**Everything else is explicitly out of scope for the preprint:** the comparison scanner, full litindex
population (beyond the `*301` quarantine, now done), more baselines. They do not change the contribution;
building them is the named follow-through risk ("finding new things to build instead of shipping").

## Drafting framing (for the preprint)
- **A falsifiable, decontaminated inference framework — Linear A as the worked null. NOT a decipherment
  paper.**
- Reduce trading jargon (DSR / "offensive" / "action cutoff") → expert-facing prose (pre-registration /
  selective inference / multiplicity / locked holdout / MDL).
- **Related work sits inside the 2025–26 conversation:** Tamburini 2025, Nair 2026 (Indus synthetic-
  baseline scorecard), the systematic Aegean/Cypriot computational review — not a reconstruction of the
  2011 lineage.
- **The one invariant to keep enforcing everywhere: grade from persisted artifacts, never from the
  agent's narration.** That rule is what caught both §8 confounds.

## Deferred to the operator (human / outward — not autonomous)
- **Recruit an Aegean epigrapher** (Davis / Duhoux / CREWS orbit) before any sign-interpretation claim
  goes public.
- **Send the DĀMOS courtesy email** (`docs/damos-courtesy-email-DRAFT.md`).
- **The publish / preprint-venue decision.**
