# Blockers and data requests at LOGOS-2 closure

Ordered by E202's generated value-of-information ranking (`VALUE_OF_INFORMATION.csv`,
copied verbatim into this package at closure by `generate_package.py`; counts are script-generated).

## 1. GORILA klasmatogram (fraction apparatus) silver ingestion — IMMEDIATE, in-house
E202 rank 1. Licensed material already held; the blocker is transcription labour + QC, not
access. Unblocks the full-power E204 metrology pass: the strict arm proved the *accessible*
web-archived apparatus is incomplete (joint infeasibility identical across all 200 permuted
nulls — a data-completeness property, not value evidence), so visible-entry arithmetic is
capped until the complete apparatus is ingested.

## 2. Anetaki II full edition — EXTERNAL, on publication
E202 rank 2 (Δlog10 value-space 7.25). Triggers: the 72h FRACTION_ORDER seal's opening
event (that seal stays closed and unread), the E213 P1/P2 prospective tests, and multi-slot
anchor candidates from a distinct lineage.

## 3. One secure toponym identification from a distinct lineage (≥4 slots) — RARE
E202 rank 3. The census is exhausted; only a new find or archival discovery supplies this.
This is the anchor-independence bottleneck named by both the 72h campaign and E212.

## 4. Historical-replay datasets — BLOCKED_DATA
Old Persian, Carian, Meroitic, Iberian replay cases could not be run in E201 for want of
machine-usable cognate/bilingual datasets with licences. The F1a harness is frozen and
would score them without re-tuning.

## 5. Stroke-order / 3D palaeographic channels — SOURCE_BLOCKED
Confirmed unavailable in prior campaigns; E205-C stayed blocked. No accessible dataset
exists; request stands with low priority (E205-I showed the graded prior is generic even
when granted).

## 6. E201-F1b — PENDING_EXTERNAL (not a blocker to closure)
Waits only on the operator's CSA sufficiency sweep (~2026-07-13). Import boundary
`imports/E201_F1B/` is armed (receipt-gated, code-free); the post-closure addendum
procedure is written and append-only. By frozen rule its result cannot alter closed
conclusions; it can only refine the E201 row and fill the named defect in the published
sufficiency curve (the clamped LB/Cypriot 600–700-word cells).

## Explicitly NOT requested
Anything that would re-run conditionally exhausted methods on the same corpus (see
`METHOD_EXHAUSTION_UPDATE.md`), and any unlicensed redistribution-restricted full text
(SM2017 remains metadata-only in the audit index).
