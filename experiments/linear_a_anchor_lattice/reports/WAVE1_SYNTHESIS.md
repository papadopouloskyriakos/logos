# Wave 1 synthesis (WP-B/D/E/F) — the new channels do not unlock the value layer

**WP-B sign universe (COMPLETE):** 3 inventories (SPLIT 166 / CONSERVATIVE 163 / MERGED 158; syllabic 134/131/126,
6% allograph band — no conclusion flips). **Decisive finding:** anchor + substitution constraint is confined to
**AB-shared** signs; the **69 genuinely-undeciphered A-only signs are image-rich (57/69 stroke) but relationally
DARK (1/69 anchor, 1/69 substitution)**; 12 syllabic signs have zero coverage on any channel (pure free variables).
26 signs have independent multichannel coverage — all AB-shared (already value-benchmarkable, non-circular only as
graded labels).

**WP-D source expansion (`SOURCE_EXPANSION_NULL`):** 0 new independent multi-slot anchors. {Egyptian-rendered
Cretan toponyms} ∩ {LA ≥4-slot toponyms} = {Phaistos} only (PA-I-TO, 3 slots/1 site — sub-threshold, and its
Egyptian rendering fixes no LA value without assuming the reading). Amnisos/Knossos/Kydonia/Lyktos are LB-only.
**EA-13 La≥4 regime UNREACHABLE** (0 of 3 required eligible anchors; binding constraint = the empty overlap, not
skeleton length; only newly-excavated LA tablets bearing Egyptian city-names could unlock it). SEED_A=0.

**WP-E stroke corpus (`SHAPE_CHANNEL_SOURCE_BLOCKED_FOR_STROKES`):** SigLA = bbox-only (no strokes/vectors/
contours/orientation; drawings never scraped). Bbox-aspect channel has no sign-identity power (AUC 0.57–0.60;
LOSO 4.68% ≈ 4.02% majority). Pipeline built; true-stroke + cross-script legs SOURCE_BLOCKED. No font substitution.

**WP-F substitution bridge (`CROSS_SCRIPT_SUBSTITUTION_BRIDGE_NO_POWER`):** F1 calibration SOLID (LB consonant axis
value-free, strengthens to 0.75 freq-disjoint, label-shuffle p=0.003 — the durable L2/L3 channel reconfirmed). But
the bridge is a POWER CEILING: it fails on the KNOWN LB↔Cypriot pair, so LA↔LB is underdetermined a fortiori (zero
Holm survivors; leave-one-sign-out recovers no class surviving multiplicity). Relative compatibility + maximal
uncertainty only; no absolute value; PHONETIC/LEXICAL/SEMANTIC NOT_EARNED.

**Convergence:** the two genuinely-new channels (stroke, substitution-bridge) plus source expansion all come up
short on the UNKNOWN (A-only) signs, which are shape-present but relationally/externally dark. Trajectory:
`ANCHOR_LATTICE_UNDERDETERMINED`. Remaining WPs (C re-run, G threshold lab, H joint inference, I all-sign, J
conditional-language, K acquisition, L 3D, M seals, N adaptive null) must establish this mechanically — not stop
at the first null.
