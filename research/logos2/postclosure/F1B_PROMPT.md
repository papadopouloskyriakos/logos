POST-CLOSURE TASK ONLY — E201-F1b addendum. LOGOS-2 is CLOSED; do not reopen any experiment
or reinterpret closed conclusions. The external CSA sweep has ended. Execute the frozen
boundary procedure: (1) run imports/E201_F1B/import_f1b.py on the sweep's results directory
on main — receipt (hashes+provenance) BEFORE inspecting anything; (2) score under the
F1a-frozen harness with zero re-tuning; (3) classify ONLY as INDEPENDENT_REPLICATION |
SCALE_EXTENSION | NON_COMPARABLE | FAILED_IMPORT | INVALID; (4) write the append-only
ADDENDUM-F1B.md per imports/E201_F1B/BOUNDARY.md — F1a stays the binding confirmatory
result; no closed conclusion changes; E208 stays de-authorized; (5) commit and push the
addendum as its own commit. If the sweep died incomplete, classify FAILED_IMPORT or
NON_COMPARABLE per the boundary rules and record that. Then stop.
