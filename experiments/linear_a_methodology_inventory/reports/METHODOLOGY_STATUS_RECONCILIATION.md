# Methodology status reconciliation

48 contradictions/stale-status flags found by the per-lineage forensic pass (data/STATUS_CONTRADICTIONS.csv). Each preserves both paths in the timeline; NONE silently resolved. Grouped by type.

## Material (affect a load-bearing number or a settled verdict)
- **[Egyptian]** EFFECTIVE-N vs NOMINAL: the '3/3' recovery is effectively n≈1 — Lyktos is a pure-identity freebie, and Knossos+Amnisos both hinge on the SAME terminal s->s(sh) rule learned from a SINGLE training record (HV-0234); leave-one-out of that one record flips the verdict 3->1 (REFUTE).
- **[admin_structure]** A_SIGN VOCAB COUNT (ADMIN-03): LINEAR_A_STRUCTURAL_CHANNEL_AUDIT reports A_SIGN vocab = 259, which conflicts with the corrected real-syllabary~=92 figure established in the separate constraint-expansion campaign (per MEMORY); 259 conflates variants/damaged forms. Cross-lineage note, not resolved within these three dirs.
- **[constraint_expansion]** SUPERSESSION direction: this campaign's Stage A INVALIDATES the earlier 12h-campaign '259 params > 212 constraints' as a category error (corrected to 92 syllabic params, count-favourable). Per orchestrator note + MEMORY, this campaign's own '259->92 + internal value-blindness' HEADLINE was itself later flagged SUPERSEDED downstream; load-bearing surviving results are the 92-syllabary recount and t
- **[di_mino]** S_morph is structurally VALUE-INVARIANT (identical 0.2738 under any relabelling) -> the PRIMARY preregistered held-out statistic has NO_DISCRIMINATIVE_POWER for the *301 value; the H1 REJECT therefore rests on the S_lex channel + combined gate, NOT on the primary statistic — a nuance easy to misread as 'S_morph rejects /na/' (it does not).
- **[di_mino]** Family A TASK_LEDGER = REJECT but the S_morph leg is explicitly NO_DISCRIMINATIVE_POWER and /na/ is rank 1 on the weakest S_phono channel; the REJECT is driven only by S_lex rank 21/65 + failing E[max]. Different channels of the SAME family give opposite surface signals (phono rank 1 vs lex rank 21).
- **[Foundry]** CORPUS-MULTIPLE CONFLICT: WP3.2b substitution power curve (FND-07) reports LA is ~1.7x below the clean regime, while WP5.2 seed-propagation power curve (FND-14) reports LA needs ~4.9x its corpus. Framed as different channels but never reconciled to one figure; both are quoted campaign-wide.
- **[Foundry]** WP5.2 low-end curve is NON-MONOTONE: data/wp5_2_seed_propagation_power_curve.json shows size 300 kv=3 AUC=0.7747 > size 539 kv=3 AUC=0.647, contradicting the report's 'smooth monotone 0.647->0.752' framing and making the '~4.9x / crosses 0.70 at ~2620 wf' point estimate noisy.
- **[Foundry]** ANCHOR INDEPENDENCE CONFLICT: data/wp5_anchor_inventory.json summary field n_held_out_survivable_candidate=26 (26 signs), but the report (WP4_WP5) and FINAL_CAMPAIGN_REPORT de-rate this to only 2 held-out survivors (I, RI) via the frozen cross-script gate — the JSON field is stale/optimistic vs the ~13x-overstatement finding.
- **[relative_phonology]** E3 cross-site contradiction: TASK_LEDGER E3 result_note and E_CROSS_SITE_GENERALIZATION.md assert 'only A- generalizes cross-site (non-HT p0.0099)', but K1 adaptive best-of-affix null downgrades this to ARTIFACT (adaptive p 0.4212) and M_FINAL_VERDICT sec.11 records it as an ERRATUM. The task-level artifact still carries the un-corrected positive; only the K/M layer carries the correction.
- **[relative_phonology]** SEARCH_RECEIPT.json is effectively EMPTY (families_searched/segmentation_variants/model_families/seed_selections all [] with note 'populated per task') despite a very large search space actually explored; the Art. VII global receipt was never populated (selection accounting lives instead inside K1_nulls.json).
- **[paper_core]** CSA sufficiency curve (PC-22): the SUBMITTED TACL #11385 sentence says 'the completed 2,000-step curve splits the branches at the Linear-A-scale locator (600-700 words)' but Exit-B CONFIRMED the deposited LA-scale values (linb 0.044 n=276, cypriot 0.066 n=346) are _interp ENDPOINT CLAMPS from the last complete lower size - the 600-700 cells were recovery_mean=null/n_seeds_done=0 and NEVER measured
- **[paper_core]** Identifiability parameter count: the 12h campaign (PC-41) + paper headline use 259 sign-value parameters > 212 constraints, but the cleaned-inventory (PC-03) + representation-audit + MEMORY establish the real conservative syllabary is V=92; MEMORY explicitly flags '259 params' as a count-favourable category error SUPERSEDED by the constraint-expansion campaign - so the headline parameter figure is
- **[paper_core]** Frozen-paper corpus anchors vs recompute (PC-35): distinct_words=650 (paper) vs direct syllabogram recompute 1021 (+57%); mean_signs_per_word=2.3 (paper) vs 1.97 recompute - definitional gaps flagged in representation_audit but the paper is byte-frozen so uncorrected (would be an ERRATUM).
- **[paper_core]** Palaeo image leg (PC-12): commit 5e82576 message calls it 'cross-script IMAGE alignment SUCCEEDS (first non-null offensive signal)' but the final finding WITHDRAWS this and relabels it a CIRCULAR shape-genealogy engineering demonstration (review P2) - old commit chronology contradicts the settled verdict.
- **[paper_core]** C.4 contamination (PC-15): the artifact contamination_rate=1.000 (all 32 defined seeds) directly contradicts the honest verdict 'the C.4 contamination delta is not yet a measurable quantity' - the 1.0 is a construction artifact, correctly reported as such but a raw-number/interpretation contradiction a naive reader would misread.
- **[paper_core]** Information floor (PC-04): finding headline 'the too-little-text excuse is false / there is enough ciphertext' was WITHDRAWN by two red-teams as an identifiability claim (conflates unique-key-exists with distinguishable-from-impostors); retained only as toy-model precondition and SUPERSEDED by the CSA learning curves.

## Stale STATUS files (headers contradict the campaign's own final verdict)
- **[Egyptian]** STALE STATUS: STATUS.md still headlines 'egyptian_channel_readiness = READY_FOR_PREREG_DRAFT ... the first channel to pass its gate' and DECISION_LOG.md stops at E6 (READY), neither reflecting the later one-shot TRIVIAL/NULL downgrade that CLOSED the phonetic-anchor programme (per CRETAN_ANCHOR_RESU
- **[admin_structure]** STALE STATUS vs final verdict (admin_schema/STATUS.md): top line still reads 'NOW (2026-07-06): SCAFFOLD created ... status=INCOMPLETE, benchmark_readiness=NOT_READY' and 'Next action: complete the prior-art & novelty audit', but the DECISION_LOG shows the programme actually advanced through Stage 5
- **[admin_structure]** DUPLICATED/HALF-TEMPLATED STAGE LEDGERS: admin_schema/STATUS.md lists Stage 5 twice (once 'FROZEN', once 'pending'); no_human_structure/STATUS.md lists Stages 3/4/5 twice (done rows above, stale 'pending' template rows below) — the pending rows contradict the COMPLETE / NO_POWER_BEFORE_MODELING fina
- **[constraint_expansion]** STALE STATUS.md vs final verdict: STATUS.md ends mid-campaign ('Next: H+J, K, L') and is dated 2026-07-07 through Stages E+F only, while CAMPAIGN.md/CHECKPOINT_3_EL.md record all stages COMPLETE with FINAL VERDICT NEW_CONSTRAINTS_BUT_NO_DECIPHERMENT (commit 2996567). STATUS never updated to COMPLETE
- **[Foundry]** ANCHOR INDEPENDENCE CONFLICT: data/wp5_anchor_inventory.json summary field n_held_out_survivable_candidate=26 (26 signs), but the report (WP4_WP5) and FINAL_CAMPAIGN_REPORT de-rate this to only 2 held-out survivors (I, RI) via the frozen cross-script gate — the JSON field is stale/optimistic vs the 
- **[relative_phonology]** STALE STATUS.md: dated 2026-07-07 it still reads as scaffold ('Next automatic task: A1_reconstruct -> A2_multiplicity') while the campaign is CLOSED (M_FINAL_VERDICT/LICENCE_STATE 2026-07-08, NEW_CONSTRAINTS_BUT_NO_DECIPHERMENT). STATUS was never updated to the terminal state.
- **[external_language]** STALE STATUS in continuity/STATUS.md: same file both declares 'CLOSED + ARCHIVED, split verdict H_exact=NULL_PUBLISHED/H_drift=NO_POWER' (header + §IX-§XV DONE row) AND leaves the pre-execution scaffold intact -- 'Verdicts (not yet issued): status=INCOMPLETE, channel_readiness=NOT_READY' with phase 
- **[external_language]** STALE STATUS in ritual/STATUS.md: 'Verdicts (not yet issued)' header contradicts the very next line status=COMPLETE / NO_POWER and the phase table (rows 3-9 unchecked but row 10 COMPLETE). Final verdict is COMPLETE/NO_POWER.
- **[external_language]** EA-12 calibration: STATUS/DECISION_LOG say the Hoch data blocker was 'LIFTED by open research' (Kilani 2019 CC-BY), but the frozen correspondence model (EA-03) was still never fit and the calibration corpus never populated -- verdict correctly stays INCOMPLETE, not NO_POWER, but the 'unblocked' fram
- **[paper_core]** Stale STATUS: the 3 parked branches' Stage-1 STATUS.md are STALE (campaign T0 + MEMORY: branches are CLOSED negatives, not Stage-1); campaign ground-truth also lists post-submission Phase-2 as REFUTE while the frozen submitted paper reports its cross-script leg as an earlier null - the frozen paper 

## Resolution principle
Where a later audit supersedes an earlier claim (position→C/V, reduced-seed, 259-params, image-leg, information-floor), the LATER artifact governs and the earlier is retained as SUPERSEDED/INVALIDATED in the lineage (append-only, Art. XVII). Frozen-paper contradictions (CSA clamps, 650-vs-1021, 259) are ERRATUM-class: recorded here, NOT edited (the paper is byte-frozen + submitted). Stale STATUS files are cosmetic (final verdicts live in CAMPAIGN.md/DECISION_LOG/TASK_LEDGER), flagged not rewritten (read-only audit).
