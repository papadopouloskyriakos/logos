# EPOCH-102 — frozen prereg slice (cross-method synthesis & frozen LA application)

**Family:** F12 CROSS_DISCIPLINARY_DECIPHERMENT (LAST epoch) · **priority:** NEXT_AFTER_MORPHOGENESIS · **layer:** L2 · **gate:** A/C
**Parent prereg:** `cross_disciplinary/PREREGISTRATION.md` (E102 slice frozen for the plan_hash).

## Question (frozen)
Intersect ONLY independently-validated outputs of E097–E101 + morphogenesis. Which relations survive the
dependency-adjusted independence audit (≥2 GENUINELY independent method families) + the absolute-value gate?

## Method (frozen)
- Candidate relation: "signs group into vowel classes." Partitions from method families on blinded LB: eigenmap
  (PCA+kmeans), ICA (FastICA+kmeans) — both on the SAME context embedding — and a **positional** channel (word
  initial/medial/final profile) with a GENUINELY DIFFERENT input.
- **Independence audit (Art. XI):** methods sharing the context embedding collapse to ONE channel. A channel
  supports the relation only if its partition recovers vowel above null (ARI > 0.05). n_independent = # distinct
  lineages that support it.
- **Absolute-value gate (all required):** ≥2 independent channels + LOO-anchor + LOO-method + held-out-form
  prediction + full adaptive null. Else RELATIVE constraints only.
- **Frozen LA application:** anonymous relative classes + per-sign class-margin (uncertainty) + ranked
  evidence-acquisition targets. NEVER values.

## Verdicts (mechanical)
`SYNTHESIS_ABSOLUTE_VALUE_SUPPORTED` (gate passes) · `SYNTHESIS_RELATIVE_CONSTRAINTS_ONLY` (≥2 independent channels
but gate fails) · `SYNTHESIS_NO_INDEPENDENT_CHANNELS` (structure rests on 1 channel) · `SYNTHESIS_NULL` (no
above-null structure).

## Scope
L2, opaque signs. Frozen LA outputs are RELATIVE/ANONYMOUS only. Forbidden + withheld: absolute sound values,
translations, language-family ID. No relation is promoted on shared-lineage pseudo-independence.
