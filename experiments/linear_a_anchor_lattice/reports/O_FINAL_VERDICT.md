# O — Final campaign verdict: Linear A anchor lattice & symmetry-breaking

**Branch:** `research/linear-a-anchor-lattice` · **Parent:** red-team-verified inventory `056e96d` ·
**Constitution:** v2.2 · **Opened:** 2026-07-08 · **Closed:** 2026-07-08 (verdict issued after WP-N).

> **FINAL MECHANICAL VERDICT:** **`ANCHOR_LATTICE_UNDERDETERMINED`** — established FOUR independent ways
> (structural C, counterfactual G, inferential H, adaptive-null N), with the anchor-threshold *quantified*, the
> discipline proven load-bearing by measurement (naive pipeline fabricates a decipherment 120/120 under null and
> fires on real data; disciplined gate 0/120 with planted-positive 20/20), and the exact acquisition that would
> move the needle named and priced. The candidate UNSAT discovery was itself null-adjudicated to `UNSAT_GENERIC`
> — the campaign refutes even its own most interesting positive. **All seven transfer licences: `NOT_EARNED`.**
> No sign value assigned.

## 1. Commits & isolation
Parent `056e96d` (red-team-verified inventory); commits `e3f1191` (scaffold+A) → `9c683c8` (B/D/E/F) →
`756b82d` (C) → `14b29a1` (G/H) → `d46a11e` (I/J/K/L/M) → _(N/O)_. Worktree isolated; `main@6fd4f20` + paper
(`a2ab89fa…`) + all prior branches untouched; 38-test baseline passed; append-only throughout; not merged.

## 2. Baseline reconciliation (WP-A)
§1 starting-state claims **verified exactly** against the red-team artifacts (215 instances / 14 lineages / 0
material errors / SEED_A=0 / EA-13 scarcity-limited / Anetaki seal unopened) — no correction needed.
`DO_NOT_REPEAT_UNCHANGED` frozen; no closed method was rerun unchanged.

## 3. Unresolved-sign universe (WP-B)
Three inventories (SPLIT 166 / CONSERVATIVE 163 / MERGED 158; syllabic 134/131/126 — a 6% allograph band, no
conclusion flips). **Decisive structure:** anchor + substitution constraint is confined to AB-shared signs; the
**69 genuinely-undeciphered A-only signs are image-rich (57/69) but relationally dark (1/69 anchor, 1/69
substitution)**; 12 syllabic signs are pure free variables.

## 4. Anchor lattice & dependency collapse (WP-C)
562 nodes (15 types) / 208 hyperedges (8 types). The Art. XI collapse is decisive: **dependency-collapsed
independent anchors = 0** — every value-bearing lineage (toponym/personal-name/gloss) descends from the single
`META_CONTINUITY_LA_eq_LB` meta-lineage; citation-stacking is clone-stacking. The graph *structurally* reproduces
the frozen-gate `REFUTE_LOTO_FRAGILE`/SEED_A=0 verdicts. 68/69 A-only signs dark (*49's lone pin is vacuous).

## 5. New source evidence (WP-D)
**`SOURCE_EXPANSION_NULL`** — 0 new independent multi-slot anchors. {Egyptian-rendered Cretan toponyms} ∩
{LA ≥4-slot toponyms} = {Phaistos} only (PA-I-TO, 3 slots/1 site, sub-threshold). Amnisos/Knossos/Kydonia/Lyktos
are Linear-B-only. No quota manufactured.

## 6. EA-13 reassessment (WP-D/G)
The La≥4 design-power threshold **reproduced and sharpened**: it is a *foothold* criterion, not a decipherment
criterion (per-anchor FP 0.043 amplifies to script FP 0.159; best config abs 0.452) — and it is **unpopulatable**
with present evidence (0 of the required ≥3 eligible anchors exist; the binding constraint is the empty
external∩LA-attested overlap, not skeleton length).

## 7. Stroke channel (WP-E)
**`SOURCE_BLOCKED`** — SigLA carries only axis-aligned bboxes (no strokes/vectors/contours/orientation; document
drawings never scraped). The one non-circular observable (bbox aspect) has no sign-identity power (AUC 0.57–0.60;
LOSO ≈ majority baseline). Ingestion pipeline built; **no font substituted for evidence**.

## 8. Substitution bridge (WP-F)
**`NO_POWER`** — the F1 calibration is solid (the LB consonant-axis is real, value-free, and *strengthens* under
frequency-disjoint holdout to 0.75 — the durable relative channel reconfirmed), but the cross-script bridge fails
even on the **known LB↔Cypriot pair** (zero Holm survivors), so LA↔LB is underdetermined a fortiori. Relative
compatibility only; no absolute value follows from this channel.

## 9. Anchor-threshold response surface (WP-G)
**`LA_AT_IDENTIFIABILITY_ZERO`.** Absolute-value recovery of an LB-scale syllabary (abs ≥0.90, FP ≤0.05) requires
**(12 anchors × 8 slots × 12 distinct lineages)**; the abs≥0.75 frontier = exactly 3 independent 8-slot anchors;
abs≥0.50 at (2×8-slot). Slots dominate; lineages bind hard; **dependency clones add exactly nothing**; incorrect
anchors are net-poisonous (f_wrong 0.1 → lattice FP 0.31). The wrong-language bench "recovers" 84–90% of signs at
FP≈1.0 — **volume is zero evidence** (the Gordon/Di Mino trap, measured). LA's operating point: abs 0.000,
eq-reduction 0.0, residual ambiguity ~10^63.4; its 3 one-toponym-deep pins price at abs 0.012 / FP 0.23 —
*worse than silence*.

## 10. Joint inference (WP-H)
**`JOINT_INFERENCE_NULL` — 3/3 solvers agree** (Bayesian factor graph, CP/weighted-SAT, MDL beam; Jaccard
0.905–1.0). A-only signs receive **0.000 absolute bits** in every solver × scenario × inventory — strictly *below*
the random-anchor null (p=0.000): the real anchor topology systematically avoids the unknowns. ≥10^270 equivalence
classes survive the gauge quotient. The MDL preference for the anchored system **flips sign under honest
dependency dedup**. Clone-stacking quantified: 9 citations of one 7.7%-reliable channel manufacture 5.92 false bits.

## 11. Per-sign results (WP-I)
**`ZERO_RETAINED_CANDIDATES` (0/151; 0/69 A-only), over-determined four ways:** 89/151 unresolved signs have zero
lattice edges of any type; max independent anchors on any sign = 1 (gate needs 3); no sign reaches 2 independent
channels (ranking never authorized); leave-one-lineage fails universally. The **random-hyperedge null retains 0 in
200/200 replicates** — no re-arrangement of existing evidence can open the gate. The zero is structural.

## 12. Retained & rejected anchors
Retained: **none** at absolute-value strength. The 5 firm toponym equations + 115 records remain catalogued as a
single-lineage L2 substrate (useful for identification, not value-decision). Rejected as independent: all
citation-clones of the continuity meta-lineage (quantified in §10).

## 13. Conditional language testing (WP-J)
**`J_NOT_AUTHORIZED` (0/3 gate criteria).** The lattice constrains A-only signs by exactly 0 bits (below the
random-anchor null), and the only nonzero reduction is conditional on the *unearned* META_CONTINUITY licence — a
tournament scored on it would test Linear B, not Linear A. No tournament run; the prior 6-family
`AT_END_TO_END_NULL` and the Di Mino `REJECT` stand uncontested.

## 14. Active acquisition ranking (WP-K)
**No accessible acquisition materially reduces the ambiguity.** Only dependency-distinct multi-slot anchors move
absolute values. Rank 1 = an Egyptian-attested toponym on a newly published ≥4-slot LA text (the only route into
the dependency-distinct Egyptian lineage): if realized, ~9 of 63.4 log10 units — leaving ≥10^54 classes. **No
single acquisition can crack the script; it can only start the anchor count toward the G-surface threshold.**
Corpus-hygiene note: the 2025 Ariadne Anetaki preliminary material is published but not yet ingested into silver
(the editio princeps proper remains unpublished).

## 15. 3D channel (WP-L)
**`SOURCE_BLOCKED`** — licence/access-blocked, not technique-blocked (MUSINT II's 43 Haghia Triada sealing models:
0 downloadable, no licence; CMS Arachne 2D-only; Heraklion permission-regime; Anetaki 2D publication photos only).
Fail-closed ingestion+validation pipeline delivered and tested (8/8 corruptions caught; synthetic joins 6/6).

## 16. Held-out & seals (WP-M)
Phonetic scoring: **`NO_CANDIDATE_TO_SEAL`** (0/163 signs exceed 0.1 absolute bit in the licensed state). No
lattice structural prediction is scoreable on SEAL_2/3/4 without contamination (every lattice finding derives from
the full corpus — correctly refused). The Anetaki editio princeps remains unpublished (verified 2026-07-08) →
existing seals unopened. **One new prospective seal registered** (`M_ANETAKI_LATTICE_DELTA_SEAL`): falsifiable L2
predictions (A-only token fraction [1,11]/119; zero new independent anchors; substitution non-rescue) whose
failure would be *informative*.

## 17. Adaptive false-positive rate (WP-N) — `ADAPTIVE_NULL_COMPLETE`
13,000 new component nulls (12 families) + 400 lattice-gate nulls + **120 full-adaptive replicates** running every
selection layer (sources × anchor-subsets × lineage-accounting × 12 stroke features × 4 bridge methods × 3 grains ×
3 solvers × 5 restarts × value-selection × thresholds × 8 lexica × best-result reporting) + 20 planted-positive
calibration reps. Results:
- **Disciplined licence-gated gate: FWER 0/120** (CP95 ≤2.46%); 0 retained candidates in 980 gate-level null
  evaluations; branch A never openable under 1,000 dependency-cloned reps (max independent anchors 2 < 3).
- **Planted-positive control: 20/20 retained** — the gate opens on genuine lineage independence. The campaign's
  `ZERO_RETAINED` is a property of the evidence (one collapsed meta-lineage), **not an unopenable gate**.
- **The naive pipeline fires always:** citation-stacking + best-of-everything fabricates a "decipherment" in
  **120/120** null replicates (~20.7 fake sign-retentions/rep; median nominal p 0.0014) — and already fires on the
  REAL data (23 signs with ≥3 stacked "channels"). The discipline is measurably load-bearing.
- **Art. XV confirmed by measurement:** the conditional-S2 0.1-bit reporting bar alone is anticonservative under
  adaptive search (10/120 standalone false positives) — conditional reductions from an unearned lineage must never
  be reporting criteria. The real value (0.000 bits) sits below all 120 nulls: `REAL_BELOW_NULL` under the full
  selection stack. BH over the campaign's 6 recorded p-values: 0 discoveries (min q 0.455).

## 18–19. Strongest surviving / failed candidates
- **DOWNGRADE (N adjudication): the continuity-pins × LA-substitution UNSAT is `UNSAT_GENERIC`, not new science.**
  Every matched random pin-set is also UNSAT (P=1.000 in all matched families), and the observed 15/24 violations
  is *fewer* than the null mean (16.3 freq-matched / 17.7 uniform; P(viol≥15)=0.77–0.93). The H finding stands as a
  mechanical fact (hard+hard has 0 solutions) but carries **no evidence against the continuity values and no
  specific conflict** — it is what any 38-pin set looks like against 24 substitution pairs. The earlier
  "new sharp negative" framing (checkpoint A–H, §10 above) is hereby superseded, append-only.
- **Strongest surviving finding:** the **quantified anchor-threshold surface itself** (G) — the first mechanical
  pricing of what a Linear A decipherment *requires* (12×8-slot independent-lineage anchors; foothold at 2–3;
  clones worth exactly zero; wrong anchors net-poisonous) — plus the measured demonstration (N) that the
  discipline separating this campaign from a Gordon/Di Mino-style false positive is load-bearing (naive 120/120 vs
  disciplined 0/120 on identical evidence).
- **Strongest failed candidate:** the anchor lattice as a value-recovery instrument at current evidence — correct
  architecture (planted-positive 20/20), pointed at a corpus that provides zero independent anchors.

## 20. Highest earned licence
**None earned or advanced.** Highest earned Linear A layer remains **L2/L3** (from prior campaigns). This
campaign's own contributions are L2 accounting results (darkness pattern, UNSAT constraint, threshold surface).
STRUCTURAL/FUNCTIONAL/SEMANTIC/LEXICAL/PHONETIC/LANGUAGE_ID/TRANSLATION: **all NOT_EARNED**.

## 21. Exact remaining uncertainty
The Linear A value layer is underdetermined at **~10^63.4 residual ambiguity** (G, operational) to **≥10^270
equivalence classes** (H, gauge-quotient lower bound over the full grid). The G surface prices the exit exactly:
**2–3 independent 8-slot anchors for a foothold (abs 0.5–0.75), 12 for recovery** — and WP-D/K establish that no
accessible source can currently supply even one. This is not an impossibility theorem: it is a measured evidence
deficit with a named, priced acquisition path (new excavated multi-slot texts bearing externally-attested names;
a bilingual; or the Anetaki delta as falsification fuel).

## 22. Final mechanical verdict
**`ANCHOR_LATTICE_UNDERDETERMINED`** — with `SOURCE_EXPANSION_NULL` (D), `SOURCE_BLOCKED` (E, L),
`NO_POWER` (F), `LA_AT_IDENTIFIABILITY_ZERO` (G), `JOINT_INFERENCE_NULL` (H), `ZERO_RETAINED_CANDIDATES` (I),
`J_NOT_AUTHORIZED` (J), `NO_CANDIDATE_TO_SEAL` (M) as the component verdicts. Candidate-ready requirements: not
approached (0 of the required independent channels).

## 23. Integrity confirmation
The submitted paper (byte-frozen `a2ab89fa…`), the verdict history, and all prior campaign branches remained
untouched. No closed method was rerun unchanged; no data fabricated; no filler commits; synthetic/known-script
successes never counted as Linear A readings; every negative recorded append-only.
