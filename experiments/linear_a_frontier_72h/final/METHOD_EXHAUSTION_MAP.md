# Method-Family Exhaustion Map (§8.1 / §12.6)

Machine-readable: `METHOD_EXHAUSTION_MAP.json` (34 frontier keys, generated from the ledger's `frontier` /
`frontier_family` fields). §8.1 vocabulary: `SUPPORTED_RELATIVE_ONLY` · `OPEN_AND_PROMISING` · `OPEN_BUT_NO_POWER` ·
`SOURCE_BLOCKED` · `COMPUTE_BLOCKED` · `CLOSED_UNDER_CURRENT_DATA` · `INVALIDATED` · `DUPLICATIVE`.
Regenerated under the corrected bucketer (terminal audit remediation, item C1).

> **Keying caveat (sharpened):** the auto-map keys on the free-text ledger `frontier` field, whose short codes do
> NOT always match the narrative family names below — e.g. ledger key `F11` is E036 (sign-frequency concordance),
> while the narrative "F11 Turing morphogenesis" is keyed `TURING_MORPHOGENESIS`, and the narrative "F12
> cross-disciplinary" is keyed `CROSS_DISCIPLINARY_DECIPHERMENT` (ledger key `F12` is the E038–E043
> prefix-paradigm block). The authoritative family-level closures below reconcile against `FRONTIER_MAP.yaml`.
> No family earned a transfer licence; **"SUPPORTED_RELATIVE_ONLY" means a relative / structural constraint
> survived held-out testing — never a value.**

## SUPPORTED_RELATIVE_ONLY (relative constraints survive held-out; no value, no licence)
- **F4 held-out robustness** — the home of the graduated finding (A-initial positional enrichment,
  E023/E024/E103/E103R).
- **F23 spatial / layout** (15 epochs) — spatial-administrative regularities survive as relative structure.
- **F3 substitution motifs** — cross-script motif geometry consistent with AB homomorphy (L2), composition-driven.
- **F10 prospective prediction** — FRACTION_ORDER seal frozen (E004); generated status `PROSPECTIVE_FROZEN`.
- **F6 temporal/scribal**, **F14/F15/F16/F17/F20/F22** — assorted relative/structural constraints, held-out where
  tested (the generated map now marks ledger-key `F14` (E044/E045/E054/E055: dominant/underpowered/deviant
  outcomes) `MIXED_SEE_EPOCHS`). **STRONG_LEAD_ADJUDICATION (E103/E103R)** — the A-initial enrichment replicated
  across segmentation variants (robustness, not independent replication).

## CLOSED_UNDER_CURRENT_DATA (bounded negatives / no power at LA scale)
- **Turing morphogenesis (`TURING_MORPHOGENESIS`, E091–E095; E096 de-authorized)** — `CLOSED_REFUTED`: mechanism
  adds nothing over equal-diffusion / generic clustering on every channel; segmentation + geographic NULL.
- **Cross-disciplinary (`CROSS_DISCIPLINARY_DECIPHERMENT`, E097–E102)** — `CLOSED_REFUTED`: ECC/BP, Potts, causal,
  TDA, network-flow all calibrate on LB, none transfers to LA; synthesis shows a single independent channel.
- **F5 administrative semantics** — role induction supported on document *structure* (L2) but word-context→channel
  refuted; no functional licence.
- **F8 information-directed acquisition** — queue maintained; genuinely-new information source-blocked (Ariadne-2025
  not yet ingested).
- **F7 register strata**, **F9 architectures (SBI)**, **F13/F18/F19 (ledger-key `F19` — the E060–E063 marker /
  template positives — now generates `SUPPORTED_RELATIVE_ONLY`; the narrative F19 negative refers to a different
  ledger key)**, **the three §12 adaptive-null families (E018/E020/E022 — now correctly `SUPPORTED_RELATIVE_ONLY`
  under the fixed bucketer: each hosts a `*_SURVIVES_ADAPTIVE_NULL` positive; the harness-validation attempts
  E019/E021 are `HARNESS_NOT_VALIDATED` → OTHER/MIXED)** — remaining entries are bounded negatives / power floors
  at LA scale.

## MIXED / SOURCE_BLOCKED
- **F2 stroke/palaeography** — `SOURCE_BLOCKED`: bbox-only in SigLA; stroke contours require facsimiles/photos not
  accessibly available. The most valuable *unblocked-if-acquired* second-channel candidate — noting its primary
  value is inventory/transcription, and a second channel is necessary but not sufficient for value assignment.
- **F1 external anchors (Anetaki)** — `SOURCE_BLOCKED`: preliminary material published, full edition not ingested.
- **CAMPAIGN_WIDE_NULL (E104 pilot / E104R certification)** and **CROSS_EPOCH_SYNTHESIS (E105)** —
  methodology/closure, not a decipherment family.

## Bottom line
Every *phonetic/value* route is `CLOSED_UNDER_CURRENT_DATA` or `SOURCE_BLOCKED`. Every *relative/structural* route
that survived is `SUPPORTED_RELATIVE_ONLY` and licenses nothing above L3. The campaign is exhausted **within current
scope**; re-opening requires new evidence (a second independent channel — necessary, not sufficient — or an external
anchor lattice), not new algorithms.
