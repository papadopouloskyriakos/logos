# Checkpoint 02 — epochs 6–8 (clock ~69h remaining; 8/18 banked)
- E006 SEED_EFFICIENCY_NOT_ACHIEVED — but the E003 open door is REVISED, not closed: estimator headroom is
  mostly spent; the channel's own SNR is the binding constraint. Next: channel-capacity bound (close/reopen
  definitively), ensemble cell, hub-stratified anchors.
- E007 ROLE_INDUCTION_NO_POWER — W-vs-C separation genuinely structural; unit-slot channel live (U .862);
  obstacle = site-convention heterogeneity. Next: arithmetic totals detector, within-site split, dedicated
  unit-slot detector.
- E008 STROKE_RECOVERY_PARTIAL — **the stroke channel is UNBLOCKED** (SigLA alpha-channel trace renders);
  epigraphic stroke graphs carry real held-out signal (MRR 0.273 vs 0.066). Superseding note filed on
  anchor-lattice (WP-E 'bbox-only' factually false). Next: full-corpus SigLA stroke sweep (fresh prereg).
- Daily rhythm satisfied: 8 substantive epochs, 2 source epochs, 3 model epochs, heavy nulls in E003/E006.
- Next wave: E009 full SigLA stroke corpus (F2), E010 channel-capacity bound + ensemble + hub anchors (F3),
  E011 unit-slot + arithmetic totals detectors (F5).
